- **`contract/README.md`'s `build --plan` row froze a shape `--plan-from`
  cannot pin, and hid `planPathMode: tokened`.** The row's field list read
  like it described `--plan-from`'s output, but `--plan-from`
  (`_MODE_PLAN`, `python/tan/commands/build_cmd.py:1487-1494`) only reads the
  caller's own plan FILE and echoes it verbatim, returning BEFORE
  `apply_plan_token_substitution` runs at `:1502` — it freezes nothing about
  the emitter `--plan` will use once tan-cli#427 lands, `emit_build_plan`
  (`python/tan/planner/buildplan.py:375`). Measured against a real board
  (`emit('build-plan', ..., board_yaml=examples/multicore/rpmsg-v2n/board.yaml)`):
  that emitter's plan carries `"planPathMode": "tokened"` and every slice's
  `env.ALP_SDK_ROOT` / `envAppendPath.{EXTRA_ZEPHYR_MODULES,PYTHONPATH}` as a
  literal, UNSUBSTITUTED `${SDK_ROOT}` token (`buildplan.py:610,620-623,636`)
  — `--plan-from` carries no guarantee either way: it passes tokens straight
  through unchanged, so a golden recorded from it pins whatever fixture it
  was handed, not this emitter's shape. The row's stated blocker was also
  wrong: `--plan` plans IN-PROCESS (`_emit_plan`, `build_cmd.py:734`, "no
  interpreter on PATH, no PYTHONPATH, no process") and `_MODE_PLAN` returns
  before token substitution — the one place a Python interpreter gets
  resolved — ever runs, so there is no Python *interpreter* spawn on this
  path; but not "no process" full stop — `emit_build_plan` calls
  `_sdk_commit()` (`buildplan.py:339-372`), which spawns a `git rev-parse
  --short HEAD` subprocess on every plan (`buildplan.py:365-366`). The two
  real blockers are two of `_emit_plan`'s **seven** `build.plan-unavailable`
  refusal sites (`build_cmd.py:734-833`): a resolvable alp-sdk checkout
  (`build_cmd.py:768`) and a `board.yaml` (`build_cmd.py:776`); the other
  five (an incomplete checkout, two planner-import failures, a `SystemExit`,
  and the catch-all) also refuse before any `data` exists.
  `contract/README.md` corrected to name both producers and record the token
  fact. Two stale "every plan the SDK emits today" comments this PR's own
  measurement disproves (`build_cmd.py:1498`, `plan_tokens.py:18`) are
  corrected alongside it — comments only, no behaviour changed.

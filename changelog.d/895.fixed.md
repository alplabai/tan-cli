- **A new gate fails the local pytest bar when `PINNED_PLANNER_ORACLE_SDK_REF`
  drifts from `PINNED_SDK_TAG`, instead of the drift surfacing only at
  merge-ref CI after review.** `#509`'s own postmortem: while `#884` was in
  review, `dev` moved `PINNED_SDK_TAG` (`#868`/`#888`, the alp-sdk fix for the
  GD32 bridge's missing `CONFIG_SPI=y`, ported into `tan/planner/`'s live
  `_CHIP_SUBSYSTEMS` table); the frozen oracle fixture and its
  `PINNED_PLANNER_ORACLE_SDK_REF` pin stayed put. GitHub tests the merge ref,
  so CI compared `tan/planner/`'s now-correct output against goldens frozen
  from the stale ref, and 19 cases across 19 boards on all three OSes failed
  after two review sign-offs. `grep -rn PINNED_PLANNER_ORACLE_SDK_REF` found
  exactly three hits, all in `parity.yml` (the postmortem comment, the
  definition, the checkout `ref:`) -- nothing compared it to `PINNED_SDK_TAG`,
  and `pin-move-verify.yml` never mentioned it.

  `python/tests/gates/test_planner_oracle_ref_tracks_pinned_sdk_tag.py`
  compares `parity.yml`'s `PINNED_SDK_TAG`, `parity.yml`'s
  `PINNED_PLANNER_ORACLE_SDK_REF`, and
  `tests/fixtures/planner_oracle/PROVENANCE.txt`'s recorded `alp-sdk ref`,
  all three of which must agree while the invariant is live. A hard failure,
  not the `tests/gates/test_sdk_pin_disagreement_warning.py` warn-only shape
  that pin pair uses -- `PINNED_PLANNER_ORACLE_SDK_REF`'s own comment says it
  is "NOT free to sit at an arbitrary pre-`#270` ref while `PINNED_SDK_TAG`
  moves", unlike `PINNED_SDK_COMMIT`, which may legitimately sit on either
  side of the parity tag.

  Guarded the way `test_planner_parity_actually_ran.py` guards the sibling
  byte-parity layer: on whether the bound `ALP_SDK_ROOT` checkout still ships
  `scripts/alp_orchestrate/__init__.py`, not a hand-maintained flag. Nothing
  bound skips, same as every other gate in this file that needs a live
  alp-sdk tree; a bound tree that has lost the package skips too, so once
  `#270` actually lands this assertion stops firing on its own, with no
  second commit required to silence it.

  Proven to fire, not merely written to: replaying the `#884` incident
  against a bound stand-in SDK (`PINNED_PLANNER_ORACLE_SDK_REF` mutated back
  to the stale `94378a05` while `PINNED_SDK_TAG` stays at the moved
  `eb96112b`) reds the gate; restoring the real pin greens it again. Fifteen
  synthetic-fixture cases on the pure `find_problems()` helper cover matching
  refs, a diverged workflow pin, an isolated single-pin lag, both directions
  diverging independently, a missing `PROVENANCE.txt`, an absent or malformed
  `alp-sdk ref` line, an absent or duplicated `PINNED_SDK_TAG` /
  `PINNED_PLANNER_ORACLE_SDK_REF`, and the post-`#270` shape (both unbound and
  bound-but-retired) staying non-trackable rather than red.

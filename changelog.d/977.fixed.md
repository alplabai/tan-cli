- **`sensor`'s and `minimal`'s vendored scaffolds named seven more alp-sdk-only
  paths bare, with no qualifier that they are not part of what `tan init`
  writes.** PR #975's review round found these siblings of tan-cli#912/#924's
  own bare-cross-repo-referent defect class but scoped them out of that PR;
  this is the follow-up. Five sites across two templates, both SKUs:
  `sensor`'s `src/main.c` (the `TMP112_ADDR_7BIT` doc-comment's
  `metadata/chips/tmp112.yaml` and `include/alp/chips/tmp112.h` mentions),
  `sensor`'s `board.yaml` (the "Customer workflow" paragraph's
  `scripts/alp_project.py` mention, and the chip-classification paragraph's
  `metadata/chips/tmp112.yaml` and `scripts/check_example_portability.py`
  mentions), `sensor`'s `prj.conf` (the same `scripts/alp_project.py`
  mention, a different file), and `minimal`'s `README.md` (a troubleshooting
  paragraph's bare `gpio-button-led`/`i2c-scanner` mentions, turned into real
  links -- `minimal` had no `DELIBERATE_EDITS` entry at all before this).
  Each named in prose (a `#`/`/* */` comment, no markdown link syntax
  applies) or turned into a real, `v0.16.0`-pinned link (`minimal`'s
  markdown), noting the referent is not part of the scaffolded project --
  `board.yaml`/`prj.conf`'s `scripts/alp_project.py` mentions instead say
  "resolved via `ALP_SDK_ROOT`", since a scaffolded project's own
  `CMakeLists.txt` genuinely invokes that script through the SDK checkout
  `ALP_SDK_ROOT` points at, unlike the purely descriptive pointers elsewhere.
  Fourteen new `tests/parity/scaffold_byte_parity.py` `DELIBERATE_EDITS`
  entries (seven substitutions x two SKUs), `MANIFEST.md`'s live-entry count
  and itemised enumeration extended to match (thirty-one -> forty-five), and
  `test_template_integrity.py`'s `_NUMBER_WORDS` extended past `thirty-one`
  to cover it. Not yet filed upstream as an alp-sdk issue; all fourteen
  entries retire the moment alp-sdk's `examples/peripheral-io/i2c-master`
  and `examples/peripheral-io/hello-world` heal the underlying prose and this
  tree is re-vendored.

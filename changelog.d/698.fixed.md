- **README's `### What a build needs` now has a Windows row, not just Linux and
  macOS.** #687 added the section but only ever enumerated `git`, `cmake`,
  `python3`, `ninja`, `xz`, `wget` on Linux and the shorter macOS subset; a
  reader on Windows -- a supported host with its own installer and CI leg --
  found nothing there for them. The section is now a per-OS list with Linux,
  macOS, and Windows as siblings; the Windows entry is left as an explicit
  `TODO(#698)` placeholder rather than a guess, and points at `tan doctor` as
  the live authority in the meantime, the same way the other two OSes already
  do. (#698)

- **tan-cli#616's faultdecode divergence from alp-sdk is CLOSED, and the tests
  that policed it now say so instead of the opposite.** Moving tan's SDK pin
  to `c07254b2` (this PR) surfaced alp-sdk `dad5b35a` ("fix(faultdecode): lead
  with the escalated fault, not the escalation (#1389)", folding #1358,
  2026-08-12), which ported tan's LSPERR/MLSPERR-is-a-cause root-cause
  branches and its address-VALID-is-not-a-cause fallback into
  `scripts/alp_cli/faultdecode.py` verbatim, and adopted tan's refusal of a
  negative fault-register value as its own behaviour (the refusal text itself
  is not verbatim -- alp-sdk raises a `click.BadParameter`
  ("Invalid value for '--cfsr': ... is negative -- fault registers are
  unsigned"), tan raises its own registered `faultdecode.invalid-register-value`
  with a longer message). That reddened the two tests that had pinned the
  divergence by name:
  `test_decode_diverges_from_the_sdk_original_only_where_tan_cli_616_declares`'s
  non-vacuity guard (`the sweep no longer exercises every declared divergence
  class: []`) and
  `test_the_sdk_original_decodes_a_negative_cfsr_and_tan_deliberately_does_not`'s
  own oracle-side assertion (`upstream no longer decodes a negative CFSR`).
  Both are stale claims of an ongoing difference that no longer exists,
  reconciled per each test's own plan rather than weakened: the root-cause
  sweep asserts byte-equality with the SDK original again, including on the
  two formerly-divergent classes, and is renamed
  `test_decode_matches_the_sdk_original_including_the_classes_tan_cli_616_used_to_diverge_on`;
  its non-vacuity guard is re-verified live (neutering the sweep's
  deterministic + random coverage down to single-bit-only reds it on
  `the sweep no longer exercises every formerly-divergent class:
  ['lazy-fp-preservation-is-a-cause']`, restored green after). The negative-CFSR
  divergence test is deleted with a note, exactly as its own docstring said it
  should be "the day upstream adopts the refusal"; its command-level parity
  coverage is restored as an exit-code-only assertion (the refusal text still
  differs), following the precedent
  `test_matches_oracle_exit_code_on_bad_hex_value` already set in the same
  file. `tan/core/faultdecode.py::_root_cause`'s docstring,
  `tests/fixtures/faultdecode_golden.PROVENANCE.txt`, and
  `tests/fixtures/faultdecode_golden.json`'s `_meta` field are updated to
  record the closure at `dad5b35a` instead of an open divergence upstream was
  asked to follow.

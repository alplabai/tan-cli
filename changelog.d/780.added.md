- **`tan.model` — the `.alpmodel` compiler-adapter engine now lives inside
  tan.** Relocated verbatim from alp-sdk's `scripts/alp_model/` (13 modules,
  1,029 lines, ADR-0028 Task 2): `build_model`, the CPU/Vela/DRP-AI/DeepX/
  ExecuTorch adapters, the CBOR manifest model, and the `.alpmodel` package
  reader/writer. The one external import (`alp_project_loader.resolve_soc_path`)
  is repointed at tan's own `tan.planner.som_metadata.resolve_soc_path`, which
  already carried the identical signature. `cbor2` joins tan's required
  dependencies (the manifest's CBOR round-trip needs it unconditionally); a new
  optional `model-io` extra (`tflite`, `flatbuffers`) carries the lightweight
  TFLite tensor-I/O reader. Nothing under `tan/commands/` consumes this package
  yet -- that in-process wiring is a follow-up.

- **`test_pin_move_verify_contract_mirror.py` no longer misdiagnoses a correct
  alp-e2e checkout as bogus.** Bound at `ALP_E2E_ROOT`, the live-drift check
  reported "this does not look like an alp-e2e checkout" for ANY checkout
  missing `alpe2e/pinverify.py` — including a genuine alp-e2e `main` checkout,
  where that file simply does not exist yet because the pin-move-verify
  receiver (alp-e2e PR #1) still lives only on the unmerged branch
  `feat/pin-move-verify-receiver`. Anyone following the gate's own documented
  procedure and binding `ALP_E2E_ROOT` at a normal clone was told their
  checkout was invalid, sending them to debug the wrong thing. The check now
  tells the two situations apart: a `root` with no `alpe2e/` package (or no
  `.git`) still FAILS with the original wording, but a real alp-e2e checkout
  that simply lacks `alpe2e/pinverify.py` now SKIPS, naming the branch the
  receiver currently lives on — a `main` checkout genuinely cannot audit an
  unmerged contract, and a gate that fails on a correct environment trains
  people to ignore it. Per tan-cli#275's rule, this is a SKIP, never a silent
  pass: the self-check and the value-level drift comparison both still FAIL on
  real drift once a checkout actually carries `alpe2e/pinverify.py`.

  This re-audit (2026-08-18) itself found **no drift**: bound at alp-e2e
  commit `fe8202890afb405e132482d9b7a23a76348bd710` — still the tip of
  `feat/pin-move-verify-receiver` — the live-drift check passes and
  `MIRRORED_CONTRACT_HASH` is unchanged. `pin_move_verify.py`'s "last
  re-audited against" note is updated to record the 2026-08-18 pass.

oracle SHA: 97ad481b94007eeb3439a21088c10c59aedc95ac  (feat(build-plan): publish envAppendPath + executionPolicy (ADR-0020 Phase 1, additive) (#847))
captured from df312cec^ (parent of ADR-0020 Phase 4 #848); has fan_out + envAppendPath/executionPolicy

Hand-reviewed field correction (alp-sdk#862, "Windows sysbuild paths" fix, H3/#596):
`connectivity_iot-fleet-ota.build-plan.json` slices[0]/[1] `command.args`'
`-DSB_CONF_FILE=...` second path segment was updated from
`<root>/build/alp_sysbuild.conf` to
`<root>/examples/connectivity/iot-fleet-ota/build/alp_sysbuild.conf` -- the
project-relative `alp_sysbuild.conf` overlay path is now anchored on the
resolved per-project `build_root`, not the emitting process's CWD, so the
overlay path is CWD-independent per the plan contract. This is the only
field touched by this correction; every other value in this fixture (and
every other oracle fixture, aside from the alp-sdk#999 correction below) is
untouched, still frozen at 97ad481b.

Hand-reviewed field correction (alp-sdk#999, "planner refuses a boardless
target", merged ff39401d): `multicore_rpmsg-imx93.build-plan.json` slices[1]
(coreId `m33`) `command` was updated from a `west build -b
alp_e1m_nx9101_m33 ...` command to `null`, and `warnings` was updated from
`[]` to carry one `board-tree-missing` warning for that slice.
`E1M-NX9101`'s `topology.m33.board:` names `alp_e1m_nx9101_m33`, which has no
tree under `zephyr/boards/alp/` -- the planner now refuses to emit a `west
build -b <board>` command guaranteed to fail with Zephyr's own "No board
named ... Invalid BOARD" error, blocking the command the same way
`no-command`/`yocto-recipe-missing` already do, rather than continuing to
hand a consumer a command frozen at 97ad481b that was never buildable. This
fixture was re-vendored whole from alp-sdk's own copy at `PINNED_SDK_TAG`
(`3ffd87743d7648b317e6b3e34fc2fbb57745e7d2`), not hand-edited here -- the
byte diff against alp-sdk's copy is empty. These two fields are the only ones
touched by this correction; every other value in this fixture, and every
other oracle fixture, is untouched, still frozen at 97ad481b.

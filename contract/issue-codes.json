{
  "schemaVersion": 1,
  "_comment": [
    "FROZEN issue codes: the exact `issues[].code` strings alp-sdk-vscode",
    "matches with `===` to gate real behaviour. Every one of those matches",
    "FAILS OPEN -- an unrecognised code is indistinguishable from 'no",
    "problem', so a rename here is silent on both sides with CI green. See",
    "alplabai/tan-cli#106.",
    "",
    "This file is the single source. Python's shipping emit sites are checked",
    "in both directions by `python/tests/gates/test_every_issue_code_is_registered.py`",
    "and `test_frozen_issue_codes.py`; `crates/tan-cli/tests/contract.rs` owns",
    "only entries whose `emittedBy` path points into the frozen Rust oracle.",
    "The release workflow folds the combined registry into `envelope-contract.json`",
    "asset so the extension's own contract test can diff against an artefact",
    "instead of a hand-copied fixture.",
    "",
    "Adding a `frozen` code is cheap. REMOVING or RENAMING one is a breaking",
    "wire change: bump the CLI MAJOR/MINOR, say so in CHANGELOG.md, and open",
    "the matching issue on alp-sdk-vscode. Do not 'fix' a rename by",
    "loosening the consumer to a prefix match -- `bootstrap.` would swallow",
    "codes it has no verdict for.",
    "",
    "A `reserved` code is the pre-consumer state: the spelling exists at the",
    "emission site (the gate still checks that) but `consumer` is \"none\" --",
    "nobody matches it with `===` yet, so renaming or dropping it costs",
    "nothing on the wire. Promote a `reserved` code to `frozen` the moment a",
    "consumer binds to it (fill in `consumer`/`consumerEffect` for real); do",
    "not invent a third status for that transition.",
    "",
    "EVERY literal emit site must appear here at some status (tan-cli#219).",
    "`frozen_issue_codes` only ever walked registry -> source, so a code that",
    "was never registered was ungated on BOTH sides at once: this repo's",
    "checks iterate the registry and never saw it, and alp-sdk-vscode's gate",
    "keys off the published artefact, which is built from this same registry.",
    "A rename of an unregistered code was invisible to both repos",
    "simultaneously. `every_emitted_issue_code_is_registered` walks the other",
    "way and fails when an emitted code has no entry. 41 codes were in that",
    "state when it landed; they are `reserved`, NOT `frozen` -- freezing what",
    "no consumer reads would over-commit and make every future internal",
    "rename a contract break for nobody's benefit.",
    "",
    "The published `envelope-contract.json` carries this array WHOLE, all",
    "three statuses, not a frozen-only subset -- so the artefact's code list",
    "is everything tan emits, and a consumer reads `status` to decide what a",
    "code promises. Keep it that way: a silently-partial artefact that",
    "presents itself as the contract is worse than either honest option.",
    "",
    "`literal` (tan-cli#372): required on a `crates/` entry, where it is the",
    "verbatim Rust needle `crates/tan-cli/tests/contract.rs` substring-checks",
    "against source. A `python/` entry must NOT carry this field at all --",
    "`test_frozen_issue_codes.py` (which owns every python-side entry, per",
    "tan-cli#363) checks emission with an `ast` parse instead and never reads",
    "it, so a value here would be mandatory-to-supply and verified by",
    "nobody. It was: 42 of the 198 python-side entries had already gone stale",
    "under that regime, unnoticed, before the field was dropped for that",
    "whole side. `contract.rs::check_or_delegate` asserts the absence, so a",
    "python-side entry cannot silently regain a value nothing will ever check."
  ],
  "issueCodes": [
    {
      "code": "bootstrap.windows-unsupported",
      "status": "retired",
      "severity": "error",
      "consumer": "alp-sdk-vscode src/alpCli/service.ts :: bootstrapHostVerdict",
      "consumerEffect": "Refuses the Windows bootstrap and offers 'Reopen in WSL'.",
      "note": "Emitted by tan v0.3.0 and EARLIER only (the retired commands/bootstrap.rs, which shelled the SDK's POSIX bootstrap.sh). Native Windows bootstrap shipped in v0.3.1, so current tan never emits it -- but the consumer branch is permanent back-compat for anyone pinned to an old binary via alpSdk.cliPath. RESERVED: this spelling must never be reused for a different verdict, which is what the gate asserts."
    },
    {
      "code": "bootstrap.yocto-host",
      "status": "frozen",
      "severity": "error",
      "consumer": "alp-sdk-vscode src/alpCli/service.ts :: bootstrapHostVerdict",
      "consumerEffect": "Refuses to bootstrap a Yocto-only project on a non-Linux host. Renamed, the project is sent into a bootstrap that cannot work here.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"yocto-host\"",
      "note": "The consumer ALSO requires severity 'error'. The mixed-board WARNING reuses this same suffix at severity 'warning' and must stay a warning -- promoting it would refuse a board that can bootstrap its Zephyr cores."
    },
    {
      "code": "bootstrap.prerequisites-missing",
      "status": "frozen",
      "severity": "error",
      "consumer": "alp-sdk-vscode src/alpCli/service.ts :: prerequisitesMissingIssue",
      "consumerEffect": "Stops the extension spawning the real bootstrap terminal after tan already refused. Renamed, the customer watches the identical failure scroll past with the install guidance lost.",
      "emittedBy": "crates/tan-core/src/bootstrap/prerequisites.rs",
      "literal": "code: \"prerequisites-missing\"",
      "note": "The `bootstrap.` prefix is applied by `failure()` in crates/tan-cli/src/commands/bootstrap/mod.rs. NOT the only refusal from that gate: `bootstrap.python-not-runnable` and `bootstrap.python-too-old` are separate codes (no missing TOOL to report) and a consumer wanting those two must match them by name."
    },
    {
      "code": "presets.sdk-root-unresolved",
      "status": "frozen",
      "severity": "warning",
      "consumer": "alp-sdk-vscode src/ideHub/newProjectFlowPanel.ts",
      "consumerEffect": "Warns that the Hardware list carries no core topology. Renamed, the New Project wizard silently falls back to its static E1M_MODULES catalogue and a HETEROGENEOUS SoM scaffolds single-core with no IPC -- the reference part E1M-AEN801 is multi-core, so that is the default path.",
      "emittedBy": "crates/tan-cli/src/commands/presets.rs",
      "literal": "\"presets.sdk-root-unresolved\"",
      "note": "Also pinned end-to-end by the `presets-no-sdk` golden envelope, which is the stronger gate: it asserts the code actually reaches the wire, not just that the string survives in the source."
    },
    {
      "code": "debug-config.legacy-entry-migrated",
      "status": "reserved",
      "severity": "info",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to surface a one-time notice that a stale pre-#155 `\"ALP: ...\"` launch-configuration entry was folded into the maintained `\"Alp: ...\"` one: any hand-resolved value on an unresolved-placeholder field (device, miDebuggerServerAddress, configFiles, ...) the customer had filled in on the orphan carried across, while every other field tan owns was refreshed to this run's values (tan-cli#133, reopened).",
      "emittedBy": "crates/tan-cli/src/commands/debug_config.rs",
      "literal": "\"debug-config.legacy-entry-migrated\"",
      "note": "Fires only on the one-time migration path in `tan_core::debug_launch::create_launch_json_write_plan`: an exact-name miss against the current `\"Alp: ...\"` name that then hits the ONE legacy spelling of that same name. It never fires when a current-named entry already exists (whether or not a legacy one also still sits in the file) -- that branch deliberately leaves any leftover legacy entry untouched rather than guessing which of two possibly-hand-edited entries is authoritative."
    },
    {
      "code": "debug-config.comments-dropped",
      "status": "reserved",
      "severity": "info",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to surface a one-time notice that a `tan debug-config` write discarded a comment (or trailing comma) sitting inside the byte span it rewrote -- the one launch-configuration entry a splice replaced, or, on the whole-document fallback, the customer's entire original file (tan-cli#182 review finding #2).",
      "emittedBy": "crates/tan-cli/src/commands/debug_config.rs",
      "literal": "\"debug-config.comments-dropped\"",
      "note": "Set from `LaunchJsonWritePlan::comments_dropped` (`tan_core::debug_launch::write_content`): for a splice, whether `strip_jsonc` changes the replaced entry's own original byte span; for the whole-document fallback, whether it changes the original file at all. Never fires on the no-op short-circuit path (an unchanged merge returns `original` verbatim) or on an append (nothing existing is ever rewritten)."
    },
    {
      "code": "debug-config.sdk-identity-overwrite",
      "status": "reserved",
      "severity": "info",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to surface a one-time notice that a `tan debug-config` write just replaced a concrete existing `device`/`targetId`/`configFiles` value with one resolved from the SDK's published per-variant debug-probe identity (alp-sdk#987) rather than from a real build (alp-sdk#1026 review finding #1).",
      "emittedBy": "crates/tan-cli/src/commands/debug_config.rs",
      "literal": "\"debug-config.sdk-identity-overwrite\"",
      "note": "Set from `tan_core::debug_launch::sdk_identity_overwrites`, called only for the field(s) `fill_debug_probe_identity_from_sdk` itself populated this run (never for a field a real build's `runners.yaml` already resolved -- that overwrite is pre-existing, intended behaviour per `merge_configuration`'s own doc comment, not something this code is scoped to disclose). Fires once per overwritten field, only on the write path (never `--preview`, which never reads or merges into the existing file at all). tan-cli#518 (Python side) narrowed this code's own emit condition for a LIST field (today only `configFiles`): it no longer fires just because the existing and incoming values differ -- the real merge (`debug_launch._merge_list_by_identity`) may instead APPEND the incoming value and leave the existing one in place, when `.alp/` provenance cannot prove that existing value was tan's own prior output, and nothing concrete was actually lost in that shape. `sdk_identity_overwrites` now mirrors that real merge decision before flagging a list field, same narrowing precedent as `debug-config.sdk-identity-key-absent` below. `debug-config.sdk-identity-appended` (also new in tan-cli#518/#982) covers the append shape this code stays silent about."
    },
    {
      "code": "debug-config.sdk-identity-key-absent",
      "status": "reserved",
      "severity": "info",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to distinguish 'this SoM's SDK-published debug-probe identity exists but does not include a value for this server's field yet' (e.g. every Alif variant today, for `openocd_config`) from the generic 'still needs resolution' case, which fires for the same reason a pre-build project has no `device` at all.",
      "emittedBy": "crates/tan-cli/src/commands/debug_config.rs",
      "literal": "\"debug-config.sdk-identity-key-absent\"",
      "note": "alp-sdk#1026 review finding #4: the generic 'Placeholder fields...' preview note is real but unspecific, and names `device` even for e.g. an OpenOCD draft that carries no `device` key at all. Fires when `fill_debug_probe_identity_from_sdk` found a `variants[].debug` block for the resolved SoC variant but the field `server_identity_field` maps to this server is still an unresolved placeholder in the draft. Emitted on BOTH `--preview` and a write -- this is advisory about resolution state, not about what a write changed on disk, unlike its `sdk-identity-overwrite` sibling above. tan-cli#489 (4) narrowed this code's own emit condition on the Python side: it no longer fires for `device` (jlink) when the reason `resolution.device` stayed unresolved is that no core id indexed the SDK's per-core `jlink_device` map at all -- `debug-config.sdk-identity-core-unresolved` (below) covers that instead, since 'the SDK publishes no value for this field' is the wrong cause to report when the lookup never had a usable key to try. `configFiles`/`targetId` are not core-keyed, so this code is unaffected for those two fields."
    },
    {
      "code": "debug-config.sdk-identity-core-unresolved",
      "status": "reserved",
      "severity": "info",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to distinguish 'device stayed unresolved because no core id indexed the SDK's per-core identity map' (no --core given and no prior build, OR an explicit --core the map has no entry for) from 'the SDK publishes no device value for this SoM at all' (`sdk-identity-key-absent`) -- the two have the same working remedy (pass --core naming a published core) but a different named cause.",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#489 (4): split off `debug-config.sdk-identity-key-absent`, which used to fire for this case too -- misattributing a missing LOOKUP KEY as a missing PUBLISHED VALUE. Reproduced on a never-built E1M-AEN801 project with no --core: `metadata/socs/alif/ensemble/e8.json` publishes `jlink_device: {\"m55_hp\": \"Cortex-M55\", \"m55_he\": \"Cortex-M55\"}`, yet the old message said the identity 'does not include a value for `device` yet', naming no remedy, when `--core m55_hp` resolves it immediately. Scoped to `device`/jlink only: `configFiles` (openocd) and `targetId` (pyocd) are not keyed by core in `variants[].debug` at all (`fill_debug_probe_identity_gaps`), so `sdk-identity-key-absent` remains correct for those. This is a faithful-port gap, not a port regression: the identical message and emit condition exist in the frozen Rust oracle (`crates/tan-cli/src/commands/debug_config.rs:203`+`:439`); this Python-only split does not touch `crates/`. Not a breaking wire change: every `debug-config.*` code in this registry is still `\"status\": \"reserved\"`/`\"consumer\": \"none\"`, so nothing binds to the old spelling this splits off from."
    },
    {
      "code": "debug-config.sdk-identity-appended",
      "status": "reserved",
      "severity": "info",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to surface a one-time notice that a `tan debug-config` write left an existing `configFiles`/`setupCommands` entry in place and APPENDED the SDK-resolved value beside it, rather than replacing it -- the accepted degradation `.alp/` content-hash provenance (tan-cli#518) falls back to whenever it cannot prove the existing entry was tan's own prior output.",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#982 review finding #2: `sdk_identity_overwrites`'s own tan-cli#518 narrowing (see that code's note above) made it correctly SILENT on this exact shape -- an unmatched existing list entry `.alp/` provenance could not confirm as tan's own is APPENDED beside the new value, not overwritten, so nothing concrete was lost and there is nothing to tell the customer to 'restore'. But that left the append itself completely undisclosed: `exit 0`, `issues: []`, and a customer with two board `.cfg`s now sourced on one TAP (the same failure class `test_a_resolved_replacement_overwrites_the_previous_one_instead_of_accumulating` names) with no signal one of them is stale. Set from `tan.core.debug_launch.sdk_identity_stranded_appends`, which mirrors the real merge decision the same way `sdk_identity_overwrites` does before flagging a field, so the two codes are mutually exclusive per field per run: a given list field on a given write fires at most one of `sdk-identity-overwrite` / `sdk-identity-appended`, never both. Python-only (tan-cli#518/#982 postdate `crates/`'s freeze, tan-cli#269); there is no Rust counterpart to port from or keep in sync with."
    },
    {
      "code": "debug-config.load-files-preserved",
      "status": "reserved",
      "severity": "info",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to surface a one-time notice that a `tan debug-config` write left an existing `loadFiles` value in place -- an explicit attach-only `[]` included -- instead of replacing it with this run's fresh resolution, because `.alp/` content-hash provenance (tan-cli#518) could not prove the existing value was tan's own prior output (tan-cli#1020 review).",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#1020 review blocker: `loadFiles` (tan-cli#945) is a list field, but NOT `configFiles`/`setupCommands`'s shape -- it names one deliberate artefact list per configuration, not a set of independently-owned entries, so appending an unproven existing value would be wrong here (cortex-debug programs every entry, and an existing `[]` means a customer chose attach-only on purpose). `tan.core.debug_launch._merge_load_files` therefore never appends: an existing value this run cannot prove is tan's own is left EXACTLY as it was, wholesale, never partially merged. This code is the disclosure for that outcome, set from `tan.core.debug_launch.load_files_preserved`, which mirrors the real merge decision the same way `sdk_identity_overwrites`/`sdk_identity_stranded_appends` mirror theirs. `loadFiles` is populated by `create_launch_draft` itself (tan-cli#945), never by the SDK-identity fallback `sdk-identity-overwrite`/`sdk-identity-appended` are scoped to, so this is a THIRD, independent mirror-and-disclose call, not a new case of either existing one. Python-only; there is no Rust counterpart (`loadFiles` postdates `crates/`'s freeze, tan-cli#269)."
    },
    {
      "code": "debug-config.metadata-schema-invalid",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#964 review (major 5): `debug-config` reads a SoM preset/SoC JSON twice -- `_sdk_published_cores` (the --core-vs-manifest guard) and `_fill_debug_probe_identity_from_sdk` (the SDK-published debug-probe identity fallback) -- both through the shared `build_output.read_sdk_som_and_soc` walk `tan size` also drives. Before this code existed, `debug-config` passed no `warnings` to either call and validated NOTHING, despite #964's own PR body claiming it inherited the WARN half of the decided rule 'transitively' through that shared reader. `message` is `<file>: <json pointer>: <what jsonschema found>`, the same shape `presets.metadata-schema-invalid`/`size.metadata-schema-invalid` use, one issue per violation, deduplicated across the two walks (both share one `warnings` list). WARN-not-refuse, matching #964's decided rule: `debug-config` already degrades a schema-invalid preset/SoC JSON to its existing placeholder behaviour -- this code makes that degrade visible, it does not change the resolved launch configuration or the command's exit code. A checkout whose bound schema file is simply absent emits nothing under THIS code -- as of tan-cli#964's review it emits `debug-config.metadata-schema-unchecked` instead (below), not silence; see `tan.core.metadata_schema.validate_document`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "debug-config.metadata-schema-unchecked",
      "status": "reserved",
      "severity": "info",
      "consumer": "none",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#964 review (major 6, 'skip-but-disclose'): the sibling `debug-config.metadata-schema-invalid` code above used to degrade a missing schema file SILENTLY (`schema_warnings` stayed empty because there was nothing to check against) -- `issues: []`, byte-identical to a validated-clean run for a `--format json` consumer. `message` is `<file>: not validated -- no schema at <path> in this checkout` (`tan.core.metadata_schema.missing_schema_note`), deduplicated across the two walks exactly like the `warning` sibling. `info`, not `warning`: nothing here says the DOCUMENT is wrong, only that tan could not check it -- the resolved launch configuration and exit code are unaffected either way. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.workspace-guard",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to distinguish the tan-cli#185 workspace-parent-guard refusal (the checkout's parent holds unrelated content and neither --workspace nor an interactive accept resolved it) from every other bootstrap refusal, so a future UI could offer its own relocation picker instead of just showing the message.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/relocate.rs",
      "literal": "code: \"workspace-guard\"",
      "note": "The `bootstrap.` prefix is applied by `failure()` in crates/tan-cli/src/commands/bootstrap/mod.rs, same as `prerequisites-missing` above. Fires on BOTH a --non-interactive/--ci/--format json refusal and an interactive decline/cancel -- the two share this one code, distinguished only by `exitCode` (2 vs 1) and by `issues[].message`, which is what a consumer without a code-level split reads instead."
    },
    {
      "code": "bootstrap.workspace-relocated",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to notice that `tan bootstrap` physically moved the customer's alp-sdk checkout (an accepted tan-cli#185 relocation prompt, or an explicit --workspace naming somewhere new) -- `data.sdkRoot`/`data.workspaceDir` already carry the new location on the wire; this is the narrative flag that it MOVED rather than simply having always been there.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"workspace-relocated\"",
      "note": "Recorded via `Log::warn` (steps.rs), which applies the same `bootstrap.` prefix on drain as every other bootstrap warning; the literal here is the bare suffix passed in, matching how `yocto-host`'s WARNING sibling (`yocto_mixed_warning`) is emitted the same way."
    },
    {
      "code": "bootstrap.workspace-invalid",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to distinguish an unusable `--workspace <path>` value (empty/whitespace-only, or a Windows drive-relative root like `/e/foo/ws` that would otherwise resolve against whichever drive the process happens to run from) from the workspace-parent-guard refusal above -- this fires BEFORE any directory listing or IO, on the value itself (tan-cli#185 review finding 3).",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"workspace-invalid\"",
      "note": "The `bootstrap.` prefix is applied by `failure()`, same as `workspace-guard` above. Emitted by `tan_core::path_guard::resolve_workspace_target`'s `Err` case; that function does no IO of its own, so this refusal never leaves anything on disk."
    },
    {
      "code": "bootstrap.print-env-workspace-conflict",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to distinguish `--print-env --workspace <path>` (a combination `tan bootstrap` refuses outright rather than rendering env lines for a directory nothing was ever moved into) from every other bootstrap refusal (tan-cli#185 review finding 7).",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"print-env-workspace-conflict\"",
      "note": "The `bootstrap.` prefix is applied by `failure()`, same as `workspace-guard` above. Fires before the `--print-env` short-circuit and before the workspace-parent guard itself -- `--print-env`'s whole contract is printing what an already-resolved workspace exports, and `--workspace` names where a NEW one goes; the two claims conflict regardless of what the checkout's parent holds."
    },
    {
      "code": "bootstrap.manifest",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise tan's own refusal of an unreadable or version-skewed `<sdkRoot>/metadata/bootstrap.json` (alp-sdk#917, tan-cli#99) and stop before spawning the real bootstrap a second time -- today an unrecognised code falls through and the customer watches the identical failure scroll past twice.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"manifest\"",
      "note": "tan-cli#111: fires at the FIRST `load_facts(&sdk_root)` call, immediately after the SDK root itself resolves -- strictly BEFORE `select_workspace`, the workspace-parent guard (tan-cli#185), and any venv/west/pip phase. A doubled run therefore costs seconds, not minutes, and leaves nothing on disk: no `.venv`, no `.west`, no relocation."
    },
    {
      "code": "bootstrap.sdk-root-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise tan's refusal to bootstrap with no alp-sdk root resolvable at all -- distinct from every prerequisite/manifest/workspace refusal in this registry, which all presuppose a resolved SDK root.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"sdk-root-unresolved\"",
      "note": "tan-cli#111: the ONLY bootstrap refusal that predates project resolution -- it reports a null `project` (both `root` and `boardYaml`), unlike every other bootstrap issue code here. Already exercised, as a deliberately NON-matching example, in alp-sdk-vscode's own test suite (`test/alpCli.service.test.js`), which is how the review confirmed it carries no real `===` binding today."
    },
    {
      "code": "bootstrap.zephyr-base-manifest-mismatch",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to explain why an existing `$ZEPHYR_BASE` workspace was NOT reused: its Zephyr checkout is on the right pin but its west manifest is not alp-sdk's own `west.yml`, so reusing it would leave every `west alp-*` extension command unknown (tan-cli#769).",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"zephyr-base-manifest-mismatch\"",
      "note": "tan-cli#111: recorded via `Log::warn` (steps.rs), the same drain + `bootstrap.` prefixing as `workspace-relocated` above. Fires from `select_workspace`, AFTER project resolution and the workspace-parent guard, once the west-topdir facts are known -- unlike `manifest` and `sdk-root-unresolved`, which both fire before it."
    },
    {
      "code": "bootstrap.python-not-runnable",
      "status": "frozen",
      "severity": "error",
      "consumer": "alp-sdk-vscode src/alpCli/service.ts :: prerequisitesMissingIssue (PREREQ_CODES)",
      "consumerEffect": "Stops the extension spawning the real bootstrap terminal after tan already refused because `python`/`python3` resolves on PATH but will not run (a Microsoft Store alias, or similar). Renamed, the customer watches the identical failure scroll past with the install guidance lost -- the same failure shape as `bootstrap.prerequisites-missing`.",
      "emittedBy": "crates/tan-core/src/bootstrap/prerequisites.rs",
      "literal": "code: \"python-not-runnable\"",
      "note": "tan-cli#111: verified bound in the read-only alp-sdk-vscode checkout -- `PREREQ_CODES` (src/alpCli/service.ts) matches this code with `Set.has()`, equivalent to `===` for this purpose, alongside `bootstrap.prerequisites-missing`. Previously documented in contract/README.md as a workaround ('a consumer that wants those two must match them by name') instead of registered here; promoted to `frozen` because a real consumer already binds to it. Carries NO `missingPrerequisites[]` entry -- a `{tool, command}` pair cannot represent 'the Python you have will not run' -- so the fix travels only in `issues[].message`."
    },
    {
      "code": "bootstrap.python-too-old",
      "status": "frozen",
      "severity": "error",
      "consumer": "alp-sdk-vscode src/alpCli/service.ts :: prerequisitesMissingIssue (PREREQ_CODES)",
      "consumerEffect": "Stops the extension spawning the real bootstrap terminal after tan already refused because the resolved Python is below the SDK tooling's floor (currently >= 3.10). Renamed, the customer watches the identical failure scroll past with the install guidance lost.",
      "emittedBy": "crates/tan-core/src/bootstrap/prerequisites.rs",
      "literal": "code: \"python-too-old\"",
      "note": "tan-cli#111: same consumer and the same `Set.has()` binding as `bootstrap.python-not-runnable`, verified in the same read-only checkout pass. Also tool-less: the tool IS present, it is the wrong version, so there is no `{tool, command}` pair and the install command travels in `issues[].message` instead."
    },
    {
      "code": "debug-config.legacy-entry-untouched",
      "status": "reserved",
      "severity": "info",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to surface a persistent notice that a leftover pre-#155 `\"ALP: ...\"` entry still sits alongside the maintained `\"Alp: ...\"` one this run updated -- so a customer whose real hand-filled values are stranded on the orphaned entry (the exact #133 symptom) has something to act on instead of `tan` reporting bare success.",
      "emittedBy": "crates/tan-cli/src/commands/debug_config.rs",
      "literal": "\"debug-config.legacy-entry-untouched\"",
      "note": "tan-cli#179: fires on the ORDINARY same-name merge path in `tan_core::debug_launch::create_launch_json_write_plan` -- an exact-name HIT against the current `\"Alp: ...\"` name -- whenever a legacy `\"ALP: ...\"` counterpart of the SAME draft ALSO still exists in the file. Distinct from `debug-config.legacy-entry-migrated`, which fires on the MISS path when the legacy entry is the one adopted. This branch deliberately still does not touch or delete the legacy entry (see `both_a_current_and_a_legacy_entry_leaves_the_legacy_one_untouched` in `crates/tan-core/src/debug_launch.rs`) -- nothing decides which of two possibly-hand-edited entries is authoritative -- it only stops being SILENT about it. Fires on every run while the leftover entry remains, not just once."
    },
    {
      "code": "bootstrap.zephyr-base-stale",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to notice that an existing `$ZEPHYR_BASE` workspace belonging to this same SDK checkout was on an older Zephyr pin and is being refreshed in place with `west update` rather than reused untouched or abandoned for a second clone elsewhere.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"zephyr-base-stale\"",
      "note": "tan-cli#111 registry audit: recorded via `Log::warn` (steps.rs), same drain + `bootstrap.` prefixing as `workspace-relocated`. Fires from `select_workspace`'s `WorkspaceChoice::Stale` arm, the sibling of `zephyr-base-manifest-mismatch` and `zephyr-base-incompatible` in the same match -- all three were reachable before this entry but only the mismatch case was registered."
    },
    {
      "code": "bootstrap.zephyr-base-incompatible",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to notice that an ambient `$ZEPHYR_BASE` was ignored outright because it is not an alp-sdk Zephyr west workspace at all (wrong pin AND no recognisable manifest), distinct from the milder `zephyr-base-stale` (right manifest, wrong pin, refreshed in place) and `zephyr-base-manifest-mismatch` (right pin, wrong manifest) cases.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"zephyr-base-incompatible\"",
      "note": "tan-cli#111 registry audit: recorded via `Log::warn` (steps.rs). Fires from `select_workspace`'s `WorkspaceChoice::Incompatible` arm; also clears `$ZEPHYR_BASE` from every child so the foreign tree cannot hijack `west init`, same as the mismatch case."
    },
    {
      "code": "bootstrap.west-config-reconciled",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to notice that `tan bootstrap` rewrote a stale `.west/config` `manifest.path` that named a different SDK checkout under the same topdir (#31), before running `west update` against it.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"west-config-reconciled\"",
      "note": "tan-cli#111 registry audit: recorded via `Log::warn`. Fires only when adopting (not reusing) an existing west topdir, immediately before the west init/update phase; its sibling `west-config-reconcile-failed` fires when the same reconcile attempt could not rewrite the pointer."
    },
    {
      "code": "bootstrap.west-config-reconcile-failed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to notice that `tan bootstrap` could not rewrite a stale `.west/config` `manifest.path` (#31) before `west update` runs -- the subsequent `west update` may then resolve the WRONG SDK's `west.yml`.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"west-config-reconcile-failed\"",
      "note": "tan-cli#111 registry audit: recorded via `Log::warn`. The failure path also suppresses the workspace-sync record that would otherwise tell `tan sdk switch` this topdir is up to date -- see the comment above `record_workspace_sdk` in the same file."
    },
    {
      "code": "bootstrap.pip-upgrade",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to notice that the venv's own `pip`/`wheel` self-upgrade reported a problem before the dependent Python installs (Zephyr requirements, SDK extras, the editable backend) ran.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/steps.rs",
      "literal": "\"pip-upgrade\"",
      "note": "tan-cli#111 registry audit: recorded via `Log::warn`, non-fatal like every pip-phase warning -- the run continues into `zephyr-requirements`/`sdk-extras`/`editable-install` regardless."
    },
    {
      "code": "bootstrap.zephyr-requirements",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to notice that installing Zephyr's own `requirements.txt` into the venv reported a problem -- the customer's venv may be missing packages a Zephyr build later needs.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/steps.rs",
      "literal": "\"zephyr-requirements\"",
      "note": "tan-cli#111 registry audit: recorded via `Log::warn` in `pip_phase`, the same non-fatal pattern as `pip-upgrade`/`sdk-extras`/`editable-install`. Only fires when the SDK's Zephyr requirements file exists on disk."
    },
    {
      "code": "bootstrap.sdk-extras",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to notice that installing alp-sdk's own Python extras (`jsonschema`, the MCUboot dev-key tooling) into the venv reported a problem.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/steps.rs",
      "literal": "\"sdk-extras\"",
      "note": "tan-cli#111 registry audit: recorded via `Log::warn` in `pip_phase`, same non-fatal pattern as `pip-upgrade`/`zephyr-requirements`/`editable-install`."
    },
    {
      "code": "bootstrap.editable-install",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to notice that the editable `pip install -e` of tan's Python backend (`alp_cli`) into the venv reported a problem -- the venv may be left without a working backend for later `tan` invocations that shell into it.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/steps.rs",
      "literal": "\"editable-install\"",
      "note": "tan-cli#111 registry audit: recorded via `Log::warn` in `pip_phase`, the last of the three non-fatal pip-extras warnings alongside `sdk-extras`/`zephyr-requirements`."
    },
    {
      "code": "bootstrap.toolchain-install",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/bootstrap_cmd.py",
      "note": "issue #474 (ADR 0021 Lane 1 P1): recorded via `Log.warn` in `toolchain_phase`/`_acquire_toolchain`/`_finish_toolchain_install`, the acquisition of the `arm-zephyr-eabi` cross toolchain via `west sdk install --install-dir`. A DELIBERATE 4th `WORKSPACE_BLOCKING` member alongside `zephyr-requirements`/`sdk-extras`/`editable-install` -- the frozen v0.4.1 Rust oracle never acquired a cross toolchain at all, so this is a Python-only addition with no oracle sibling, not a parity gap. Covers every non-fatal failure mode the phase can hit: an unreadable/malformed `metadata/toolchains.json`, missing 7-Zip on native Windows, insufficient disk (a preflight against the manifest's own measured footprint), a `west sdk install` failure, a post-install version mismatch, a compiler that will not run, a failed stamp write, and an adopted `$ALP_TOOLCHAIN_ROOT` directory tan will not delete on the caller's behalf. Never fired for an unsupported host (Intel Mac, windows-arm64) or a manifest publishing no artifact for this host -- ADR 0021's own words, \"a coded, honest skip, never a failure\" -- those are `log.line`, not `log.warn`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise the generic fatal-step failure `tan bootstrap` reports when a REQUIRED step (venv creation, `west init`/`update`, or a hard I/O error) fails outright, as opposed to the non-fatal `Log::warn` warnings above. Distinct from every `failure()`-emitted `bootstrap.<code>` refusal in this registry: those fire before any step ran and report a `null`/pre-resolution project where relevant, this one keeps the resolved project + paths from however far the run got.",
      "emittedBy": "crates/tan-cli/src/commands/bootstrap/mod.rs",
      "literal": "\"bootstrap.failed\"",
      "note": "tan-cli#111 registry audit: unlike every other `bootstrap.*` code here, the full dotted string is written at the call site (`fatal()`) rather than a bare suffix prefixed by `failure()`/`Log::take_issues()` -- this is the one bootstrap code whose message varies per failure (whatever the failed step's own error was), so no single `consumerEffect` narrative fits every occurrence."
    },
    {
      "code": "debug-config.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise `tan debug-config`'s refusal of an invalid `--target-kind`/`--server` combination, a malformed existing `.vscode/launch.json` the merge could not parse, or (Python-only, tan-cli#456) an omitted `--target-kind` this project's own `build/system-manifest.yaml`/`board.yaml` cannot resolve to one target class -- exits `InternalFailure` (5) with a `zephyr-mcu`/`none` placeholder target rather than the resolved one.",
      "emittedBy": "crates/tan-cli/src/commands/debug_config.rs",
      "literal": "\"internal-failure\"",
      "note": "tan-cli#111 registry audit: routed through the shared `failure_envelope` (the same `debug-config.<code>` formatting `write-failure` uses); its sibling reserved codes (`legacy-entry-migrated`, `legacy-entry-untouched`, `comments-dropped`) are all `info`-severity success-path notices, not failures -- this registry had no `error`-severity debug-config entry at all before this audit. tan-cli#456 review: widened `consumerEffect` to cover the inference-refusal shapes `_run` added under this SAME code (`tan/commands/debug_config_cmd.py::_internal_failure`, still the shared `_failure` prefix site already counted below) rather than minting a new one -- both are still `--target-kind`-shaped refusals at the same exit code, and no consumer binds to this code yet to be broken by the wording change."
    },
    {
      "code": "debug-config.write-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise `tan debug-config`'s refusal when creating `.vscode/` or writing `launch.json` itself hits a filesystem error (permissions, a read-only mount, disk full) -- exits `WriteFailure` (3), preserving the resolved target/server unlike `internal-failure`.",
      "emittedBy": "crates/tan-cli/src/commands/debug_config.rs",
      "literal": "\"write-failure\"",
      "note": "tan-cli#111 registry audit: same shared `failure_envelope` path as `internal-failure`; the two are distinguished only by which exit code and text lines `debug_config.rs` passes in, matching the write-vs-internal split `crates/tan-cli/src/exit.rs` documents."
    },
    {
      "code": "debug-config.build-manifest-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise `tan debug-config`'s refusal when `--target-kind` is omitted, `board.yaml` declares a real SoM (`som.sku`), and `build/system-manifest.yaml` does not exist yet -- exits `ValidationFailure` (2): the caller's own precondition (run `tan build` first, or pass `--target-kind` explicitly), never a tan-side crash.",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#462: split off `debug-config.internal-failure`, which used to cover this case (Python-only, tan-cli#456) at exit `InternalFailure` (5) -- the wrong verdict for a precondition the command can name and the caller can fix, the same distinction tan-cli#262 settled for `tan validate`. Not a breaking wire change: every `debug-config.*` code in this registry is still `\"status\": \"reserved\"`/`\"consumer\": \"none\"`, so nothing binds to the old spelling this splits off from."
    },
    {
      "code": "debug-config.project-not-found",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise `tan debug-config` refusing a `--project` that names a directory which does not exist -- exits `ValidationFailure` (2).",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#476: the command used to CREATE the directory and write a `launch.json` into it at exit 0 with `issues: []`, because the writer calls `mkdir(parents=True)`. A deliberate divergence from the Rust oracle, which does the same thing (measured); no parity CASE pins it, since all four frozen `debug-config` argvs run in an existing work_dir. Granularity note (tan-cli#508 review): this earns its OWN code rather than folding into `debug-config.invalid-argument` below, because a missing/non-directory `--project` is checked before target/server are even parsed -- it is a precondition on the WORKSPACE, the same class as `build-manifest-missing` above, not a flag-value shape check. Covers HALF (a) of tan-cli#476 only; half (b) is `debug-config.target-kind-unresolved` below."
    },
    {
      "code": "debug-config.target-kind-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise `tan debug-config` refusing a project that offers NOTHING to infer `--target-kind` from -- no `build/system-manifest.yaml` and no `board.yaml` declaring `som.sku` -- exits `ValidationFailure` (2), naming the four target kinds the caller can pass instead.",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#476 half (b), the other half of `debug-config.project-not-found` above. #508 stopped a non-existent `--project` being CREATED and written into; a directory that ALREADY exists but is not a project (the \"ran it from the wrong cwd\" case the issue names in its own words) still received an `Alp: Native Sim Debug` launch configuration at exit 0 with `issues: []`, because `parse_target_kind(None)` defaults to `native-host`. That default is only defensible where the project says so, which `infer_target_kind` answers with a real `NATIVE_HOST` of its own (every slice a build produced is native_sim) -- this code covers ONLY its `(None, None, None)` no-signal answer, so a project with any signal at all is untouched and `--target-kind native-host` still asks for that draft on purpose. Its own code rather than folding into `debug-config.build-manifest-missing`: that one fires when the project DOES declare a real SoM and only the build is missing (remedy: `tan build`), whereas this fires when there is no evidence of a project at all (remedy: `--target-kind`, or a different directory). A deliberate divergence from the Rust oracle, which exits 0 with the native-host draft (measured: `target/debug/tan --format json debug-config --project <empty-dir> --preview`); no parity CASE and no frozen conformance golden pins it, since all five frozen `debug-config` argvs pass `--target-kind` explicitly and never reach the inference path. Not a breaking wire change: every `debug-config.*` code in this registry is still `\"status\": \"reserved\"`/`\"consumer\": \"none\"`."
    },
    {
      "code": "debug-config.invalid-argument",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise `tan debug-config` refusing a flag VALUE outside the accepted set -- `--target-kind`, `--server`, an unsupported target+server pairing, `--svd`, `--gdbserver-address` -- at `ValidationFailure` (2).",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#477: these five all reported `debug-config.internal-failure` at exit 5, which tells the user tan crashed and tells CI a typo is a tool defect. tan-cli#462 made that argument for the four PRECONDITIONS and reclassified them; the argument-validation half was not in that change, so `--core bogus` -- quoted in #462's own body -- still exited 5 in v0.5.1. A deliberate divergence from the Rust oracle (measured: exit 5); the frozen parity CASE is retired in the same commit with the reason recorded. Exit 5 stays for the `except Exception` backstop and a malformed EXISTING launch.json."
    },
    {
      "code": "debug-config.core-unknown",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise `tan debug-config`'s refusal when an explicit `--core` names no core this project has -- whether `--target-kind` was omitted (inferred, then refused by `infer_target_kind`'s own guard) or given explicitly (tan-cli#489 (5): `infer_target_kind` never runs on that path, so the SAME check is applied again once target/server are resolved) -- exits `ValidationFailure` (2), naming the cores it could have meant. Two ordered authorities behind one code (tan-cli#477 major 2): a `build/system-manifest.yaml`, when one exists, names the cores the build actually produced; with no manifest at all the SDK's published core list for this `board.yaml`'s `som.sku` stands in, and the message names those instead.",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#462: split off `debug-config.internal-failure`, which used to cover this case (Python-only, tan-cli#456) at exit `InternalFailure` (5) -- the caller's own typo or a stale `--core` value, not a tan-side crash. Not a breaking wire change: every `debug-config.*` code in this registry is still `\"status\": \"reserved\"`/`\"consumer\": \"none\"`, so nothing binds to the old spelling this splits off from. tan-cli#489 (5) widened this entry's own `consumerEffect` (not a new code) to also cover `_explicit_core_unknown_failure`'s call site: the pre-fix behaviour for `--target-kind` EXPLICIT + a `--core` matching no slice was silent exit 0, an unresolved `device`, and an `executable` pointing at a path this project's build never produced -- `debug-config.core-unknown` was already the right code for 'this cause', just unreachable from that one call path. tan-cli#477 major 2 widened the same entry once more, again with no new code: #489's guard only fired when a build manifest existed, so with NO manifest an unknown `--core` still exited 0 with `device` left as the literal `<resolved-device>` -- a launch.json that looks valid and fails later in the debugger. #508 recorded that half as deliberately open because `--core` pre-build also selects which core's SDK-published debug-probe identity to resolve (alp-sdk#1026), which 'a guard keyed only on \"does a manifest exist\" cannot tell apart from a typo without also consulting the SDK's own published core list'; it now consults it (`_sdk_published_cores`: the SoC JSON's `cores[].id` unioned with the `variants[].debug.jlink_device` keys, off the same `--sdk-root` the identity fallback already reads). Both arms refuse only what they can PROVE unknown -- no slices and no resolvable SDK core list means 'cannot be asked', which stays silent. Server-independent, unlike the `sdk-identity-*` info codes: measured pre-fix, `--server openocd` reported only `sdk-identity-key-absent` and `--server pyocd` reported `issues: []`."
    },
    {
      "code": "debug-config.target-kind-ambiguous",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise `tan debug-config`'s refusal when `--target-kind` is omitted and this project's own `build/system-manifest.yaml` names more than one target class (a mixed-core board, e.g. E1M-V2N101/V2M101's `a55_cluster`+`m33_sm`) with no `--core` to narrow them -- exits `ValidationFailure` (2), naming the target classes in play.",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#462 review round: split off `debug-config.internal-failure`, which used to cover this case (Python-only, tan-cli#456) at exit `InternalFailure` (5) -- worse than the pre-build/bad-`--core` cases split off first, since it hits a fully built, CORRECT project and never stops, not just one command early. Not a breaking wire change: every `debug-config.*` code in this registry is still `\"status\": \"reserved\"`/`\"consumer\": \"none\"`, so nothing binds to the old spelling this splits off from."
    },
    {
      "code": "debug-config.no-debuggable-target-class",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "consumerEffect": "None today. Reserved for alp-sdk-vscode to recognise `tan debug-config`'s refusal when `--target-kind` is omitted, hardware slices exist in this project's own `build/system-manifest.yaml`, but none of their `os` values maps to a known target class (e.g. a lone `os: linux` slice) -- exits `ValidationFailure` (2), naming the unmapped `os` value(s).",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#462 review round: split off `debug-config.internal-failure`, which used to cover this case (Python-only, tan-cli#456) at exit `InternalFailure` (5) -- a knowledge/version skew between tan and the SDK, not a crash: no invariant was violated and the command still produces a coherent verdict with a working remedy (`--target-kind` explicit). Not a breaking wire change: every `debug-config.*` code in this registry is still `\"status\": \"reserved\"`/`\"consumer\": \"none\"`, so nothing binds to the old spelling this splits off from."
    },
    {
      "code": "build.manifest-write-failed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/build/execute/mod.rs",
      "literal": "code: \"build.manifest-write-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.sdk-switch-pristine",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/build/execute/mod.rs",
      "literal": "code: \"build.sdk-switch-pristine\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.pristine-skipped",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/build/execute/mod.rs",
      "literal": "code: \"build.pristine-skipped\"",
      "note": "Added by tan-cli#183: `tan build --pristine` has three paths that correctly decline to wipe (an overridden `-d`/`--build-dir`, a cwd outside `build/`, and a dir that was never configured) and all three used to be silent, so a customer who asked for a clean build got an incremental one and was told nothing. Registered `reserved` per tan-cli#219's rule that every literal issue code under crates/ appears in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.sdk-switch-pristine-failed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/build/execute/mod.rs",
      "literal": "code: \"build.sdk-switch-pristine-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.configure-cache-reset",
      "status": "reserved",
      "severity": "info",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build/execute.py",
      "note": "Added by tan-cli#655: `tan build` was reporting success while silently ignoring a newly-added `app.overlay` (or a removed one, or a new `boards/<board>.conf` fragment) on an already-configured project -- CMake's own `DTC_OVERLAY_FILE`/`CONF_FILE` cache (set the first time Zephyr's `configuration_files.cmake` auto-discovery ran, `if(NOT DEFINED ...)`) never re-discovers a file that did not exist at that first configure, no matter how many times tan reconfigures afterward. `execute._maybe_reset_stale_configure_cache` detects the discoverable set changing (a same-process fingerprint, not a plan field) and appends `-UDTC_OVERLAY_FILE -UCONF_FILE` to the next configure; this code carries that reset into the JSON envelope (mirroring `build.sdk-switch-pristine`'s own reasoning) so a `--format json` caller sees it too, not only the `on_output` stream. Python-only: no `crates/` equivalent exists or ever will (crates/ is frozen). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.slice-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/build/execute/mod.rs",
      "literal": "code: \"build.slice-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.toolchain-root-unresolved",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/build/plan_modes.rs",
      "literal": "code: \"build.toolchain-root-unresolved\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.unknown-backend",
      "status": "reserved",
      "severity": "error or warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/build/execute/mod.rs",
      "literal": "code: \"build.unknown-backend\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it. Severity is per-slice and policy-driven: error under executionPolicy.unknownBackend=fail, warning under skip."
    },
    {
      "code": "cli.parse-error",
      "status": "frozen",
      "severity": "error",
      "consumer": "alp-sdk-vscode src/alpCli/service.ts :: cliUsageErrorDump",
      "emittedBy": "crates/tan-cli/src/main.rs",
      "literal": "code: \"cli.parse-error\"",
      "note": "Registered by tan-cli#219; promoted reserved -> frozen by tan-cli#885. The old note said \"pre-consumer -- nothing in alp-sdk-vscode matches this code\"; alp-sdk-vscode#575 ended that. cliUsageErrorDump matches the exact spelling, classifyOutcome raises severity on it (exit 2 alone cannot separate a rejected argv from a real validate failure), and planCliOutcome routes the WHOLE notification on it -- customer sentence, Update CLI / Run Doctor actions, and output-channel-vs-toast. The match FAILS OPEN, so a rename is silent on both sides. Frozen with the narrow meaning it now has: \"the command line was not accepted\", NOT \"a generic failure with no better code\" -- this registry's own history records new-som and faultdecode refusals reaching consumers under this code before tan-cli#399, and interrupted runs before cli.interrupted existed. emittedBy still names the deleted crates/ path, as every other frozen entry does: test_frozen_issue_codes.py carries the real Python site in FROZEN_LOCATIONS, and repointing all 70 crates/-owned entries is that gate's own documented follow-up, not this change.",
      "consumerEffect": "Reports \"this build of the tan CLI doesn't accept the command the extension sent\", raises the outcome to error severity (exit 2 alone reads as a board.yaml validation warning), routes the usage dump to the output channel and offers Update CLI. Renamed, the customer sees the raw click usage dump in a warning toast again."
    },
    {
      "code": "completion.shell-unsupported",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/completion.rs",
      "literal": "code: \"completion.shell-unsupported\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/doctor.rs",
      "literal": "code: \"doctor.internal-failure\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.server-compatibility",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/doctor.rs",
      "literal": "code: \"doctor.server-compatibility\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "envelope.serialize-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/envelope.rs",
      "literal": "code: \"envelope.serialize-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.boot-order-unknown-core",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/flash/mod.rs",
      "literal": "code: \"flash.boot-order-unknown-core\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.confirm-required",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/flash/mod.rs",
      "literal": "code: \"flash.confirm-required\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.dpidr-preflight-unarmed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Added closing tan-cli#520's review round (design point): the read-only DPIDR preflight (flash_args.expect_dpidr) stays OPTIONAL -- no shipped alp-sdk preset carries a SW-DP ID for tan to require, and tan is forbidden from deriving one (the I-26 reasoning _resolve_jlink_device documents) -- but a confirmed, real write with expect_dpidr absent used to run with no wrong-board guard AND no signal that it had none. EMISSION CONDITION, as of tan-cli#609: a real, confirmed write on a method flash_plan.DPIDR_GUARD_COVERAGE marks True -- i.e. one whose J-Link Commander session tan composes itself, so expect_dpidr would arm a real check -- with expect_dpidr absent. That is alif_mram_jlink (Flow D) and swd_probe's J-Link arm today; swd_probe's openocd/pyocd arm is excluded because an armed preflight is impossible there, and the other five registered methods because tan composes no probe session for them. #520 shipped this scoped to swd_probe alone, which #609 measured leaving the AEN's Flow D MRAM write -- the customer-facing flash path -- emitting no advisory at all; do not re-derive the scope from a method name, read the table (tests/gates/test_dpidr_guard_coverage.py pins it to the backend registry). The message names the entry's own method and gives that method's remedy (Flow D pairs expect_dpidr with jlink_device; swd_probe arms off expect_dpidr alone). Letting a --format json consumer tell an unguarded write from an ordinary clean flash without making the field mandatory. Deliberately a warning, not an error: the write itself is not wrong, only unconfirmed as to which board received it. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.entry-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/flash/mod.rs",
      "literal": "code: \"flash.entry-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.nothing-matched",
      "status": "reserved",
      "severity": "error or warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Originally registered by tan-cli#219 against crates/tan-cli/src/commands/flash/mod.rs; tan-cli#807 moved the emit site to python/tan/commands/flash_cmd.py (the Python port) and split the severity: `error` (exit 1) when a `--core`/`--helper` filter was supplied and matched nothing -- a mistyped filter used to warn/exit 0 even though it never touched a device; `warning` (exit 0, unchanged) when no filter was supplied at all, i.e. an empty/unfiltered manifest with no filter to have mistyped (`python/tests/commands/test_flash_command.py::test_unfiltered_empty_manifest_still_only_warns`). No `literal` field (tan-cli#372): nothing reads it for a python/ entry. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.slice-not-built",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/flash/mod.rs",
      "literal": "code: \"flash.slice-not-built\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.emit-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/generate.rs",
      "literal": "code: \"generate.emit-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "image.bundle-write-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/image.rs",
      "literal": "code: \"image.bundle-write-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "image.helper-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/image.rs",
      "literal": "code: \"image.helper-missing\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "image.helper-skipped",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/image.rs",
      "literal": "code: \"image.helper-skipped\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "image.slice-skipped",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/image.rs",
      "literal": "code: \"image.slice-skipped\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "image.slice-unsafe-name",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/image.rs",
      "literal": "code: \"image.slice-unsafe-name\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.would-overwrite",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/init/from_example.rs",
      "literal": "code: \"init.would-overwrite\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.write-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/init/response.rs",
      "literal": "code: \"init.write-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "inspect.board-yaml-missing",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/inspect.rs",
      "literal": "code: \"inspect.board-yaml-missing\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "inspect.path-not-found",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/inspect.rs",
      "literal": "code: \"inspect.path-not-found\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "pinmux.no-target",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/pinmux.rs",
      "literal": "code: \"pinmux.no-target\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "pinmux.schema-version-unsupported",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/pinmux.rs",
      "literal": "code: \"pinmux.schema-version-unsupported\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "pinmux.sdk-root-unresolved",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/pinmux_cmd.py",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. `crates/` was deleted by tan-cli#601; the real emit site is now `pinmux_cmd.py`'s pinmux-context resolution, fixed by this PR (tan-cli#497 defect 7) to name the rejected `--sdk-root` value via `rejected_sdk_root_message` rather than the old bare string. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "pinmux.table-empty",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/pinmux.rs",
      "literal": "code: \"pinmux.table-empty\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "pinmux.table-not-found",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/pinmux.rs",
      "literal": "code: \"pinmux.table-not-found\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "pinmux.unknown-sku",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/pinmux.rs",
      "literal": "code: \"pinmux.unknown-sku\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "scaffold.would-overwrite",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/scaffold.rs",
      "literal": "code: \"scaffold.would-overwrite\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "scaffold.write-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/scaffold.rs",
      "literal": "code: \"scaffold.write-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.bootstrap-recommended",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/sdk.rs",
      "literal": "code: \"sdk.bootstrap-recommended\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.install-not-ready",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/sdk.rs",
      "literal": "code: \"sdk.install-not-ready\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.west-config-not-reconciled",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/sdk.rs",
      "literal": "code: \"sdk.west-config-not-reconciled\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.west-config-reconcile-failed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/sdk.rs",
      "literal": "code: \"sdk.west-config-reconcile-failed\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.west-config-reconciled",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/sdk.rs",
      "literal": "code: \"sdk.west-config-reconciled\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/support_bundle.rs",
      "literal": "code: \"support-bundle.internal-failure\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.server-compatibility",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "crates/tan-cli/src/commands/support_bundle.rs",
      "literal": "code: \"support-bundle.server-compatibility\"",
      "note": "Registered by tan-cli#219, which added the emit-site direction of the gate: every literal issue code under crates/ must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.enclosing-west-workspace",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/bootstrap_cmd.py",
      "note": "The `bootstrap.` prefix is applied by `_refusal()`; fires when the intended west topdir sits under an ANCESTOR directory that already has its own `.west` (tan-cli#284's `enclosing_west_workspace_refusal`), distinct from the `workspace-guard` sibling above it (an OCCUPIED relocation target, not an ancestor workspace). Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/bootstrap_cmd.py",
      "note": "Two sites share this spelling: `_refusal(ExitCode.INTERNAL_FAILURE, \"internal-failure\", ...)` for the unreachable `check_prerequisites` fallthrough, and a literal `Issue(\"bootstrap.internal-failure\", \"error\", ...)` in the command's own catch-all `except Exception` backstop. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.python-floor-skew",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/core/bootstrap.py",
      "note": "Built by `python_floor_skew_warning()` as a bare `(code, message)` pair, prefixed to `bootstrap.` when `Log.warn(*skew)` drains it; fires whenever the manifest's declared `pythonMinVersion` and the effective (Zephyr-enforced) floor disagree, success or not (tan-cli#300). Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.python-newer-than-verified",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/core/bootstrap.py",
      "note": "Built by `python_ceiling_warning()`, prefixed via `Log.warn(*ceiling)`; warns (never refuses) when the resolved interpreter is newer than `PYTHON_CEILING_KNOWN_GOOD` (tan-cli#285's other half -- a too-NEW Python is not a guaranteed failure the way too-OLD is). Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.venv-unusable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/core/bootstrap.py",
      "note": "Built by `posix_venv_unusable()` (Linux only: `python3` runs but its `venv` module cannot create a usable environment because `ensurepip`/`python3-venv` is missing, tan-cli#161/#294); forwarded to the wire through TWO sites -- `bootstrap_cmd.py`'s `Issue(f\"bootstrap.{refusal.code}\", ...)` and `doctor_cmd.py`'s `code=f\"bootstrap.{venv_refusal.code}\"` -- both prefixing the same bare `PrereqFailure.code`. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.adopted-venv-unusable",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/bootstrap_cmd.py",
      "note": "The `bootstrap.` prefix is applied by `Log.warn`; fires when the workspace was ADOPTED from the user's own `$ZEPHYR_BASE` tree and its `.venv` is not usable. bootstrap refuses rather than reclaiming a directory it did not create. Severity is `warning` because it travels through `Log.warn`; the run's FAILING verdict is carried alongside it by `bootstrap.failed` at severity `error`, and the two share one message -- distinct from `venv-unusable`, which is the HOST `python3-venv`/`ensurepip` refusal from `python/tan/core/bootstrap.py`. Registered by tan-cli#390, which stopped `ensure_venv` from deleting a venv it did not create. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.venv-probe-inconclusive",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/bootstrap_cmd.py",
      "note": "The `bootstrap.` prefix is applied by `Log.warn`; fires when `_probe_venv_pip` could not run to completion (unspawnable interpreter, permission error, or a PROBE_TIMEOUT_S timeout), so nothing is known about the venv's pip. The venv is REUSED as-is: no answer is not a verdict and must not authorise an rmtree. Registered by tan-cli#390, which stopped `ensure_venv` from deleting a venv it did not create. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.venv-recreated",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/bootstrap_cmd.py",
      "note": "The `bootstrap.` prefix is applied by `Log.warn`; announces that a venv in a workspace bootstrap OWNS was removed and recreated because its pip ran and reported itself broken -- the one delete path that SURVIVED tan-cli#390's fix (the ADOPTED case is `bootstrap.adopted-venv-unusable`, a refusal instead of a delete). Was a `log.line` before tan-cli#390, so the deletion never reached `issues[]` and was invisible to a `--format json` consumer. Registered by tan-cli#390, which stopped `ensure_venv` from deleting a venv it did not create. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.workspace-orphan-refused",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/bootstrap_cmd.py",
      "note": "The `bootstrap.` prefix is applied by `_refusal()`; fires when `--workspace` would relocate a checkout that is the MANIFEST REPOSITORY of a live west workspace, leaving that workspace's `.west/config` pointing at nothing. Distinct from `enclosing-west-workspace`, which probes ancestors of the DESTINATION rather than the workspace the checkout is taken out of. Registered by tan-cli#389, which stopped `--workspace` from renaming the manifest repository out of a live west workspace. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.workspace-relocation-rolled-back",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/bootstrap_cmd.py",
      "note": "tan-cli#284: fires from `rollback_relocation_after()` when a LATER phase (venv/west) fails after the checkout was already relocated, and the rollback itself is reported -- whether the move-back and the pointer restore both succeeded, only the pointer restore failed, or the move-back itself could not complete. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "bootstrap.metadata-schema-invalid",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/bootstrap_cmd.py",
      "note": "tan-cli#964 review: `bootstrap` reads `sku`'s SoM preset (`_read_som_topology` -> `presets_cmd.parse_som_preset`) to decide which host can build the project and to scaffold its topology -- so, unlike `tan presets`'s WARN-and-continue half of the same rule, a schema-invalid preset here is the REFUSE half: `read_board_runtimes`'s `warnings` collects every `som-preset-v1.schema.json` violation `parse_som_preset` finds, and `bootstrap()` refuses via `_refusal()` (the `bootstrap.` prefix) before `runtimes` is used for the Yocto gate or anything is written -- same `does not validate against som-preset-v1:\\n  - <violation>` shape `tan.planner.loader._refuse_on_schema_errors` uses for `build`/`generate`. A checkout whose bound `metadata/schemas/som-preset-v1.schema.json` is simply absent emits nothing at all -- see `tan.core.metadata_schema.validate_document`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "cli.interrupted",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/cli.py",
      "note": "tan-cli#491 defect 4: the ONE envelope a `--format json` run interrupted by Ctrl-C reports. SIGINT does not reach `tan.cli.main` as a raw `KeyboardInterrupt` -- Typer catches it and re-raises `Exit(130)`, which Click turns into `sys.exit(130)` -- so before this code existed an interrupted run was indistinguishable from any other non-zero exit with no envelope and fell into the `cli.parse-error` fallback: an envelope asserting the COMMAND LINE was invalid for a run that was already programming a board, at an `exitCode` of 130, outside the contract's fixed 0-5 set. The envelope reports `exitCode` 1 (`ExitCode.RUNTIME_FAILURE`) and the process exits 1 to match, since the wire invariant is `process exit code == envelope.exitCode` (tan-cli#327); TEXT mode is untouched and still exits 130 through Click's own machinery. A DELIBERATE divergence from the frozen v0.4.1 oracle, which has no SIGINT handler at all and simply dies from the signal with zero bytes on stdout -- the port cannot copy that and still keep `stdout is the envelope channel`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "cli.command-deferred",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/deferred_cmd.py",
      "note": "tan-cli#260: shared by all seven verbs this build stubs (`scaffold`, `completion`, `diff`, `pinmux`, `inspect`, `trace`, `support-bundle`), each real in the v0.4.1 oracle and deferred to v0.6.0. Assigned to a module constant and referenced by name at the `Issue(...)` call site, not spelled inline -- see this module's own docstring for why one shared code, not seven, and why `contract/` being open again is what unblocks promoting this note. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "The port's catch-all `except Exception` backstop -- an uncaught exception reported as a coded envelope instead of a bare traceback. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.missing-tool",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "Severity is `\"error\"` when the slice actually failed and `\"warning\"` when it was only skipped -- both share this one code, distinguished by `issues[].severity`. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.nothing-built",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "Every slice was skipped rather than any slice failing outright -- a distinct code from the `build.slice-failed` sibling beside it. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.unsupported-build-root",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "tan-cli#566: the plan's top-level `buildRoot` names a tree other than `build`, so the run would materialise, dispatch and write `system-manifest.yaml` somewhere `tan flash`/`size`/`image` never look -- all three anchor on `<project>/build/system-manifest.yaml`. Measured on `dev` before the fix: a valid plan with `buildRoot: \"out\"` was ACCEPTED, wrote `out/system-manifest.yaml`, and left `build/system-manifest.yaml` byte-identical to the previous run's (md5-verified), so a following `tan flash` would program the PREVIOUS run's artefact onto silicon. The oracle (`tan 0.4.1`) already refused this at RuntimeFailure; neither the check nor the code was ported. Accepted spellings are a PATH-COMPONENT match, measured ten ways against the oracle's live-emit native path: `build`, `build/` and `./build` pass; `out`, `build/sub`, `BUILD` (case-sensitive), the empty string, `..`, `../build` and an absolute path are refused -- so `build_cmd._build_root_is_consumer_default` compares `Path(...).parts`, not strings. NATIVE mode only, also measured: the oracle accepts a non-`build` root under `--materialise` and `--plan`, which write no manifest and so carry none of the hazard. Escaping shapes were already caught downstream by `materialise.confine_to_build_root`; the gap this closes is exactly the plain-relative non-`build` name. Registered per tan-cli#224's Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.app-dir-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "tan-cli#483: `cores.<id>.app` resolved to a directory that does not exist. Before this code, a bad `app:` was never refused at all -- `tan/planner/orchestrator.py`'s `_zephyr_app_dir` (FROZEN to that unit) falls back to the app dir's PARENT the moment its own `CMakeLists.txt` probe fails to find one at the configured path, which for a nonexistent path resolves to the project root, silently, naming neither the configured path nor the substitution -- so `west build`/`cmake` ran against the wrong source tree and failed deep inside CMake with no path back to the real cause. Checked in `build_cmd._missing_app_dirs`, held back from dispatch the same shape a `${TOOLCHAIN_ROOT}` demotion is (`_dispatch`), so ONE slice with a bad `app:` fails without taking a project's other, valid cores down with it. Always `\"error\"` (not policy-driven skip/warn, unlike the missing-tool/unknown-backend siblings beside it): a bad `app:` is a plan defect, not a host-provisioning gap, and letting it skip forward silently is the exact failure this code exists to stop. EXISTENCE only -- the schema text (`metadata/schemas/board.schema.json` `$defs/core_entry/properties/app`) also asks for `prj.conf`/`CMakeLists.txt` inside the directory, deliberately not enforced: 96 of 105 enabled zephyr/baremetal core entries across alp-sdk `dev`'s own examples name `app: ./src`, a sources-only directory with neither file, which is the shipped convention `_zephyr_app_dir`'s own parent-fallback exists to serve (review round: independently re-counted at 105, not 104 -- one newer example entry, not a defect in the original count). A relative `appDir` is anchored on `--build-root` (the plan's own path-anchoring contract, issue #596), never the tan process's own CWD; a slice the planner already refused a command for (`command: null`) is never checked, since nothing was going to dispatch for it either way; an existing non-directory path is reported \"is not a directory\", not \"does not exist\". The surviving silent substitution -- `_zephyr_app_dir`'s exists-but-no-`CMakeLists.txt` parent fallback, `tan/planner/orchestrator.py:397-401` -- is tracked separately as tan-cli#517 (`planner/**` is out of this code's scope). Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.app-dir-substituted",
      "status": "reserved",
      "severity": "info",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "tan-cli#517: `cores.<id>.app` names a directory that EXISTS but carries no `CMakeLists.txt`, so `tan/planner/orchestrator.py`'s `_zephyr_app_dir` (FROZEN to that unit) silently substituted its PARENT -- naming neither the configured path nor the substituted one anywhere in the envelope. On a multi-core project that parent is the project root, which is very often the OTHER core's application, so a typo in one core's `app:` that still resolves to an existing directory built the wrong source tree and reported success. The sibling half -- an `app:` that does not exist at all -- is `build.app-dir-missing` (tan-cli#483). This one is deliberately NOT a refusal and NOT an error: the fallback is the shipped convention, 96 of 105 enabled zephyr/baremetal core entries across alp-sdk `dev`'s own examples are `app: ./src` (sources-only, `CMakeLists.txt` one level up), so refusing it would break 91% of the SDK's examples -- tan-cli#517 asks for the substitution to be ANNOUNCED, not stopped. Severity is `info` rather than `warning` for that same measurement: it fires on the CORRECT, shipped shape on nearly every build, and a per-build `warning` in a consumer that renders `issues[]` on an ok build would be an alarm on the normal case rather than a signal (`debug-config.legacy-entry-migrated` is the same shape -- a silent substitution made visible). It never changes the exit code, never holds the slice back from dispatch, and is emitted only for a slice that actually dispatches: a `${TOOLCHAIN_ROOT}` demotion or a `build.app-dir-missing` refusal on the same index wins instead, so the two never disagree about one slice. `zephyr` only -- `os: baremetal` assembles `cmake -S <app>` from `_resolve_app_path` with no `CMakeLists.txt` probe, so it has no substitution to announce. Registered by tan-cli#224's Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.cross-drive-workspace",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "tan-cli#697: a project and its `tan bootstrap`-created Zephyr workspace on two different Windows DRIVE LETTERS. `execute.py`'s tan-cli#307 `_pin_west_workspace` pins `west build`'s own spawned cwd to the resolved workspace so west's ancestor-`.west` walk lands on the right one -- but `west build`'s own source-directory sanity check (`_sanity_check_source_dir`, upstream `scripts/west_commands/build.py:534`, frozen third-party code this repo does not own) then calls `os.path.relpath(self.source_dir)` with no explicit `start`, which defaults to the pinned WORKSPACE cwd, not the project. On Windows `os.path.relpath` RAISES rather than returning a path when its two arguments live on different drive letters (`ValueError: path is on mount 'E:', start on mount 'C:'`, reproduced verbatim in the issue) -- a raw traceback previously reached the user with only the generic `build.slice-failed` in the envelope. There is no fix reachable from tan's own side of the seam (`west` is an upstream pip dependency, not `crates/`/`tan/planner`), so `tan.core.plan_exec.cross_drive_source_refusal` (pure argv/path logic, no IO -- moved out of `execute.py` to keep that command/IO module's own size down) REFUSES the slice before `west` is ever spawned, naming both mounts; `build_cmd._cross_drive_issues` promotes that per-slice reason into this coded top-level issue. Registered by tan-cli#224's Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "clean.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/clean_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "clean.manifest-unreadable",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/clean_cmd.py",
      "note": "Best-effort: an unreadable or unparsable system-manifest.yaml is a warning, never fatal -- `clean` must not fail over a manifest it only consults for an optimisation. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "clean.remove-failed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/clean_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "clean.sdk-root-not-found",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/clean_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "clean.unsafe-build-root",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/clean_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "clean.unsafe-target",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/clean_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "examples.sdk-root-unresolved",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/examples_cmd.py",
      "note": "Added by tan-cli#400. `tan examples` used to answer an unresolvable alp-sdk checkout with rc 0, `ok:true`, `data.examples: []` and `issues: []` -- byte-identical to a healthy SDK that ships no examples, so alp-sdk-vscode's example picker rendered an empty list with nothing to react to and `tan init --from-example <sourceDir>` was unreachable with no indication of why. Deliberately the same `<command>.sdk-root-unresolved` spelling `presets`, `pinmux`, `build`, `generate`, `init`, `model` and `trace` already use for this identical bad path. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change; promote to `frozen` (filling in consumer/consumerEffect for real) the moment the picker binds to it. NOT emitted when a checkout resolves and simply has no `examples/` tree: that is not a misconfiguration, and its absence is what distinguishes the two empty catalogues on the wire."
    },
    {
      "code": "examples.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/examples_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.entries-skipped",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Added closing tan-cli#487 defect 7: `_flash_entry` can legitimately skip a target it dispatched to (an unresolved `TBD` flash_arg, no flash_method, or a missing tool under `--skip-missing-tools`) -- a real per-entry diagnosis already in `data.entries[]` -- but the aggregate 'did anything flash' check only knew about the planner's OWN skip buckets (`plan.refused`/`plan.refused_skipped`), which an entry-level skip never populates, so it fell through to `flash.nothing-matched` on a run that carried no `--core`/`--helper` filter and DID match a target. This code names the real cause instead. Deliberately a warning, not an error: `ok`/exit code stay SUCCESS for this shape (see `test_tbd_sentinel_never_reaches_a_flasher` and its siblings in `python/tests/commands/test_flash_command.py`) -- unlike the build-skip sibling `flash.nothing-flashed` below, an entry-level skip is not a run that failed to program a real target, it is a target that was never a customer flash target to begin with (an unfinished helper's wiring, a host missing a tool it was told to tolerate). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.nothing-flashed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.recovery-flash-armed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Added closing tan-cli#611. A helper MCU whose SoM preset declares `flash_policy: recovery_only` is programmed by Alp Lab in production and is customer-flashable ONLY to recover a bricked device, with Alp Lab-supplied binaries. `tan flash` declines it on an ordinary run; it is reached only by `--helper <name> --recover`, and this code is emitted when that combination lets a write through. Deliberately a warning, and the entry's own status/rc are untouched: the write is the sanctioned path, not a failure -- what the code carries is that a normally-declined target was authorised, which an `ok` entry's prose alone is not something a `--format json` consumer can key off. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.sdk-root-not-found",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.slice-skipped",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.swd-probe-write-unconfirmed",
      "status": "retired",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "RETIRED by tan-cli#732, which removed the swd_probe flash backend entirely -- GD32 programming is separating out of tan (the GD32's field-update path, update_channel: alp_ota_spi_bridge, is untouched and stays; this was swd_probe's local-write path only). This code was the machine-readable half of a caveat on an swd_probe ELF/HEX write whose core never halted (added closing tan-cli#540; see the git history of this entry for the full mechanism, now deleted with _swd_probe_qualified_message/_swd_probe_halt_markers/jlink_commander_script). Pre-consumer -- nothing in alp-sdk-vscode matched this code, so retiring it is not a breaking wire change. RESERVED: this spelling must never be reused for a different verdict, which is what the frozen/retired gate asserts."
    },
    {
      "code": "generate.in-process-unavailable",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.metadata-schema-unchecked",
      "status": "reserved",
      "severity": "info",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "tan-cli#964 review (major 6, 'skip-but-disclose'): before this code existed, a `metadata_root` whose `som-preset-v1.schema.json`/`soc-spec-v1.schema.json` is simply absent made `tan.core.metadata_schema.validate_document` degrade SILENTLY to `[]` -- indistinguishable on the wire from a validated-clean run (`{ok: true, exitCode: 0, issues: []}` either way), even though `tan generate --target os-topology` (measured) then writes `build/generated/os-topology.json` from a wholly unvalidated SoC spec. This code makes that skip visible: `message` is `<file>: not validated -- no schema at <path> in this checkout` (`tan.core.metadata_schema.missing_schema_note`), one issue per DISTINCT note across every target the run rendered (deduplicated, so several targets sharing one missing schema report it once, not once per target). `info`, not `warning`: nothing here says the DOCUMENT is wrong, only that tan could not check it -- the actual `*.metadata-schema-invalid` `warning` codes (`presets`/`size`/`debug-config`) are unaffected and still fire when a schema IS present and a document fails it. Never fires for the REFUSE-worthy case (a present-but-invalid document, or a present-but-corrupt schema file) -- those still raise/refuse exactly as before. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "image.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/image_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.example-missing-board-yaml",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.example-som-unsupported",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Registered by tan-cli#890. `--from-example` retargets --som onto the copied board.yaml without consulting the SDK scaffold catalog's supported.som_skus, so tan answered ok:true for pairs `alp_project.py --emit scaffold` refuses outright (measured: sku 'E1M-AEN301' on multicore-mailbox, rc=1 there, exitCode 0 here). A WARNING rather than a refusal, matching this path's own precedent -- refusing an example outright once made --from-example unusable for nearly the whole AEN family. Emitted only when a catalog record exists for the example AND --som is outside its declared set; every 'cannot tell' (no catalog, no record, unreadable) is silent. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.hw-rev-not-buildable",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Registered by tan-cli#743. A scaffolded board.yaml sets no explicit `hw_rev:`, so it resolves, at validate/build time, to its SoM preset's own `default_hw_rev:` -- and for E1M-NX9101 that default is `status: tbd` in `metadata/e1m_modules/imx93/hw-revisions.yaml`, so `tan init --som E1M-NX9101 --template minimal-app` answered exitCode 0 with no issue while the very next `tan validate` hard-errored `sdk-compat: SoM E1M-NX9101 hw_rev 'r1' exists but is not buildable (status: 'tbd')` (rc=2). `validate` was not wrong; `init` had resolved the identical SoM preset and said nothing. A WARNING rather than a refusal, deliberately: `minimal-app` is the one template every OTHER template's own refusal message names as the escape hatch ('vendor-neutral and scaffolds any SoM'), so refusing here as well would leave a preliminary SoM with no self-contained scaffolding path at all. Emitted only when `--sdk-root` resolves and `--board-yaml` was not given -- every 'cannot tell' case is silent, matching `init.example-som-unsupported`'s own contract. tan-cli#1008 review: the SKU/hw_rev judged are read off the PLANNED board.yaml itself (`tan.core.scaffold.vendored_som`), not `--som` -- so a bare `--from-example`/`--topology` with NO `--som` at all warns too, when the example's own unretargeted board.yaml already names a not-buildable SKU/hw_rev pair; and `retarget_board_yaml_som` drops a CROSS-family retarget's stale sibling `hw_rev:` (an INTRA-family retarget, e.g. `E1M-AEN801` -> `E1M-AEN301`, keeps it -- tan-cli#1008 review round 4 -- since the value is still a real declared revision for the new SKU there) rather than leave a SKU/hw_rev pair no family table declares, so a cross-family retarget falls back to (and is judged against) the NEW SoM's own `default_hw_rev:` in that case, PROVIDED the sibling `hw_rev:` drop actually fires -- tan-cli#1008 review round 6: the writer's own `sku:`/`hw_rev:` child-key match used to diverge from the reader's (a space before the child key's colon, `hw_rev :`/`sku :`, matched `vendored_som` but not `retarget_board_yaml_som`'s literal `startswith`), so this fallback was not unconditional before the shared `_split_child_key` helper fixed it; round 3 flagged this same \"always\" phrasing as too strong for the identical reason. Deliberately still silent: an explicit `hw_rev:` that is not even a KNOWN key in its family's table -- that is `revision_known()`'s separate question (`tan validate`'s own \"not a known hardware revision\" refusal), not this warning's; it only ever names a revision the SDK metadata recognizes and marks not-buildable. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "kconfig.emit-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/kconfig_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "kconfig.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/kconfig_cmd.py",
      "note": "Catch-all backstop mirroring `presets_cmd.py`'s own, added by tan-cli#396. Before it, a board.yaml that is not valid UTF-8 raised `UnicodeDecodeError` out of a guard that only caught `OSError` (UnicodeDecodeError is a ValueError), so `tan kconfig --format json` exited 1 with ZERO bytes on stdout and a PyInstaller traceback on stderr -- and `contract/README.md` records that the vscode prj.conf symbol feed fails OPEN on an unparseable answer, so the menu rendered stale or empty with no error on either side. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` the moment a consumer binds to it."
    },
    {
      "code": "kconfig.no-sdk-root",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/kconfig_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "kconfig.no-workspace",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/kconfig_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "kconfig.parse-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/kconfig_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.build-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.unknown-subcommand",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "monitor.failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/monitor_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "monitor.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/monitor_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "presets.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/presets_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "presets.metadata-schema-invalid",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/presets_cmd.py",
      "note": "tan-cli#964: emitted once per JSON-Schema violation found while `tan presets` reads a `metadata/e1m_modules/*.yaml` SoM preset or the `metadata/socs/**` SoC JSON its `silicon:` ref resolves to, against `som-preset-v1.schema.json`/`soc-spec-v1.schema.json` respectively. `message` is `<file>: <json pointer>: <what jsonschema found>` (e.g. `metadata/socs/ae822.json: cores/2/type: 7 is not of type 'string'`), never the bare string 'schema validation failed'. WARN-not-refuse is deliberate (#964's decided rule): `tan presets` already tolerates a missing/malformed SoM detail and degrades the affected field to its existing empty sentinel (`type: \"\"`, `allowedOs: []`) -- this code makes that degrade VISIBLE, it does not change what gets listed or the command's exit code. A checkout whose bound `metadata/schemas/*.schema.json` is simply absent (an SDK predating it, or a synthetic/partial root) emits nothing under THIS code -- as of tan-cli#964's review it emits `presets.metadata-schema-unchecked` instead (below), not silence; see `tan.core.metadata_schema.validate_document`."
    },
    {
      "code": "presets.metadata-schema-unchecked",
      "status": "reserved",
      "severity": "info",
      "consumer": "none",
      "emittedBy": "python/tan/commands/presets_cmd.py",
      "note": "tan-cli#964 review (major 6, 'skip-but-disclose'): before this code existed, a `metadata_root` whose `som-preset-v1.schema.json`/`soc-spec-v1.schema.json` is simply absent made `read_soms` degrade SILENTLY -- `schema_warnings` (and therefore `presets.metadata-schema-invalid` above) stayed empty because there was nothing to check against, which is `issues: []`, byte-identical on the wire to a validated-clean run. The review measured that the customer this protected -- an SDK checkout that predates the schema -- does not exist in the released fleet (every tagged alp-sdk `v0.6.0`..`v0.16.0` ships both schemas). `message` is `<file>: not validated -- no schema at <path> in this checkout` (`tan.core.metadata_schema.missing_schema_note`), one issue per DISTINCT note (deduplicated: several SoMs sharing one missing schema report it once, not once per SoM). `info`, not `warning`: nothing here says the DOCUMENT is wrong, only that tan could not check it. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "run.exec-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/run_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "run.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/run_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "run.manifest-stale",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/run_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "run.native-sim-unavailable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/run_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.fetch-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "The `sdk.` prefix is applied by `_fail()`. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "The `sdk.` prefix is applied by `_fail()`. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.network-required",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "tan-cli#351: fires when `sdk list` is run without `--online` -- now a `warning` on a SUCCESS envelope (exit 0, `ok: true`), not an `error` on a failure. This port gates the network call the oracle itself reaches unconditionally (the oracle has no `--online` flag at all), for hermeticity; that gate is a normal, everyday state -- not a verdict on anything the caller did wrong -- so it must not exit non-zero, matching `sdk current`'s exit-0 answer to the same shape of question (`sdk-current-no-sdk`). Was `severity: \"error\"`, exit 1, emitted through `_fail()`'s `f\"sdk.{code}\"` prefixing helper (`literal: code=\"network-required\"`) until #351; now a direct literal `Issue(...)` call, still covered by the same emit-site gate under the plain-literal scan instead of the prefixing one. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.discovery-divergent",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/core/sdk_discovery.py",
      "note": "Emitted when `resolve_sdk_root_ladder` (narrow) and `resolve_sdk_root_wide` (wide) resolve DIFFERENT alp-sdk checkouts from one cwd while both label their answer `sourceTier: \"discovery\"` -- so `tan generate` can emit board files from one checkout's metadata while `tan build`/`doctor`/`kconfig` plan against another, undetectably. The message names both roots in a fixed order and says which one THIS command used, so two envelopes describing one collision correlate on the code plus the path pair. Registered by tan-cli#407. Currently emitted from `tan build` only; the remaining ladder call sites are one-line adds beside their existing `project_pin_issue(...)` calls and are tracked on that issue. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` the moment a consumer binds to it."
    },
    {
      "code": "sdk.not-ported",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "The `sdk.` prefix is applied by `_fail()`; `sdk install`/`sdk switch` refuse outright in this build (tan-cli#305). Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.project-pin-unresolved",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/core/sdk_discovery.py",
      "note": "tan-cli#263: shared by every caller of `resolve_sdk_tiered` (not just `sdk current`) when `.alp/sdk-path` names a checkout that no longer resolves and the ladder fell through to another tier. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.global-default-foreign-project",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/core/sdk_discovery.py",
      "note": "tan-cli#464: `sdk current` warns when the `globalDefault` tier answers for a caller that is under neither the `writtenFor` project the machine-global `~/.alp/sdk-default` pointer was last written for, nor the SDK path it names -- the pointer is shared and last-writer-wins across every project that ever relocates a checkout on the host, so a second project's bootstrap used to silently repoint an earlier project's `sdk current`/`tan build` at the wrong checkout, `ok: true`, `issues: []`. Resolution is unchanged by this warning; only the silence about WHOSE answer it is closes. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.unknown-subcommand",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "The `sdk.` prefix is applied by `_fail()`. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.remove-missing-argument",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "tan-cli#790: `sdk remove` with no `<version|path>` argument at all -- the `sdk.` prefix is applied by `_fail()`; `remove-missing-argument` is the literal suffix at that call site. Flat (one dot total), not the nested `sdk.remove.missing-argument` the issue's own prose sketches -- `test_issue_code_registry_shape.py`'s `ISSUE_CODE_SHAPE` (verbatim from alp-sdk-vscode's `findFrozenCodes`) hard-requires exactly one dot, so a genuinely nested code would red the consumer's own contract gate. Registered by tan-cli#224's Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.remove-outside-root",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "tan-cli#790's own proposed refusal (spelled flat, one dot -- see `sdk.remove-missing-argument`'s note for why): refused because the resolved target sits outside `--destination`'s cache root and `--force` was not given -- the footgun guard the issue asks for (\"a remove that can be aimed at an arbitrary directory is a footgun in a script\"). A bare version name can never trip this; only an explicit path argument can (`sdk_removal.is_outside_cache_root`). `--force` clears it. Registered by tan-cli#224's Python-side emit-site gate. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.remove-active",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "tan-cli#790's own proposed refusal (spelled flat, one dot -- see `sdk.remove-missing-argument`'s note for why): refused because the target is currently load-bearing -- the ACTIVE resolution for this workspace (`resolve_sdk_tiered`), the machine-global default (`~/.alp/sdk-default`), or a project's own registered pin (`~/.alp/sdk-defaults.json`, tan-cli#466) -- and `--force` was not given. The message names every reason that applies, not just the first (`sdk_cmd._load_bearing_reasons`); `--force` clears it. Registered by tan-cli#224's Python-side emit-site gate. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.remove-is-cache-root",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "tan-cli#790 review follow-up, same PR: refused because the resolved target IS `--destination` exactly -- every install under the cache root at once, not one of them -- and `--force` was not given (`sdk_removal.is_cache_root_itself`). Found live by measurement during review: `sdk.remove-outside-root` deliberately answers false for `target == destination` (a caller can name the root on purpose), and `sdk.remove-active`'s load-bearing ladder only ever names a specific version subdirectory, never the root that holds them -- so before this code existed, `tan sdk remove .` from inside an otherwise-empty cache root deleted every install with `ok: true` and no `--force` required. `--force` clears it, same as the other two footgun refusals. Registered by tan-cli#224's Python-side emit-site gate. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.remove-in-use",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "tan-cli#790's own proposed shape: the removal failed because another process holds a handle open under the target (POSIX `EBUSY`/`ETXTBSY`, or Windows `ERROR_SHARING_VIOLATION`/`ERROR_LOCK_VIOLATION` -- `sdk_removal.classify_removal_error`). The message names the specific path that was locked, not just the top-level target, when the underlying `OSError` carried one. Registered by tan-cli#224's Python-side emit-site gate. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "sdk.remove-permission",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/sdk_cmd.py",
      "note": "tan-cli#790's own proposed shape: the removal failed on a permission/attribute `dir_removal.remove_dir`'s own read-only retry (file AND parent-directory write bits, plus the Windows long-path prefix) could not clear -- everything `sdk.remove-in-use`'s handle-based classification does not cover (`sdk_removal.classify_removal_error`'s default arm). Registered by tan-cli#224's Python-side emit-site gate. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "size.budget-unknown",
      "status": "reserved",
      "severity": "info",
      "consumer": "none",
      "emittedBy": "python/tan/commands/size_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "size.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/size_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "size.metadata-schema-invalid",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/size_cmd.py",
      "note": "tan-cli#964: emitted once per JSON-Schema violation found while `tan size` reads a slice's SoM preset or SoC JSON to resolve its FLASH/RAM budget, against `som-preset-v1.schema.json`/`soc-spec-v1.schema.json` respectively. `message` is `<file>: <json pointer>: <what jsonschema found>`, deduplicated across slices that share the same SoM (a heterogeneous SoM's cores all read the same preset/SoC JSON). WARN-not-refuse (#964's decided rule): `tan size` already degrades a bad preset/SoC JSON to `budget: unknown` (see `size_cmd._resolve_slice_budget`) -- this code makes WHY visible, it does not change the measured rows or the command's exit code. A checkout whose bound schema file is simply absent emits nothing under THIS code -- as of tan-cli#964's review it emits `size.metadata-schema-unchecked` instead (below), not silence; see `tan.core.metadata_schema.validate_document`."
    },
    {
      "code": "size.metadata-schema-unchecked",
      "status": "reserved",
      "severity": "info",
      "consumer": "none",
      "emittedBy": "python/tan/commands/size_cmd.py",
      "note": "tan-cli#964 review (major 6, 'skip-but-disclose'): the `size.metadata-schema-invalid` sibling above's own last sentence used to be the vacuous-gate shape the review measured against -- a `metadata_root` with `som-preset-v1.schema.json`/`soc-spec-v1.schema.json` simply absent degraded `schema_warnings` to `[]` SILENTLY, `issues: []`, byte-identical to a validated-clean run. `message` is `<file>: not validated -- no schema at <path> in this checkout` (`tan.core.metadata_schema.missing_schema_note`), deduplicated across slices sharing one SoM exactly like `size.metadata-schema-invalid`'s own dedup. `info`, not `warning`: nothing here says the DOCUMENT is wrong, only that tan could not check it -- the measured budget rows and exit code are unaffected either way. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "size.over-budget",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/size_cmd.py",
      "note": "Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.board-yaml-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "The `validate.` prefix is applied by the local `fail()` closure. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.board-yaml-unreadable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "The `validate.` prefix is applied by the local `fail()` closure. Added by tan-cli#498 defect 3, splitting the `--offline` read failure (a directory named board.yaml, a cp1252/latin-1 file, a permission error) off `validate.internal-failure` and off exit 5. Exit 5 said `tan` had crashed, for an error the inline comment on the same line already called the user's to fix, and disagreed with the SPAWN path, which reports the identical file as exit 2 `validate.failed` (measured, both directions). Now exit 2 (VALIDATION_FAILURE) with `data.outcome: \"failed\"`, which is what this command's own outcome->exit mapping says that outcome is worth; text mode prints its own verdict line (`validate: board.yaml could not be read`) rather than \"validation failure\", since nothing was validated -- the same treatment tan-cli#350 gave `validate.board-yaml-missing`, its sibling in the file-level-problem family. The frozen Rust oracle keeps exit 5 here; this is a deliberate divergence in the family of tan-cli#262 and commit 49a5fde. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "The `validate.` prefix is applied by the local `fail()` closure. ONE call site since tan-cli#498 defect 3 moved the unreadable-board.yaml case out to `validate.board-yaml-unreadable` at exit 2: `validate_board_text` raising something that is not a `BoardShapeError`, i.e. a genuine tan bug, which is what exit 5 means. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.schema-violation",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "The `validate.` prefix is applied by the local `fail()` closure (the `BoardShapeError` path) and, separately, by `Issue(f\"validate.{result.outcome}\", ...)` -- on the OFFLINE path `result.outcome` is only ever `OUTCOME_SCHEMA_VIOLATION` (\"schema-violation\") there, since a clean result carries no messages to iterate. Since tan-cli#376 the same template also carries it for a SPAWNED validator that exited 1 without a traceback (a real schema violation); the three other spawn outcomes have their own codes. Registered by tan-cli#224, which added the Python-side emit-site gate (python/tests/gates/test_every_issue_code_is_registered.py): every issue code python/tan emits -- literal or assembled by a prefixing helper -- must appear in this registry at some status. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.sdk-root-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "The `validate.` prefix is applied by the local `fail()` closure. Added by tan-cli#376, which ported the SDK-validator spawn path: this is the oracle's own pre-spawn guard 2 (`crates/tan-cli/src/commands/validate.rs::run_spawn`), reached when no alp-sdk checkout resolves for a default (non-`--offline`) run. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.python-too-old",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "The `validate.` prefix is applied by the local `fail()` closure. The oracle's own pre-spawn guard 3 (`crates/tan-cli/src/commands/validate.rs:124-129`, message body `crates/tan-cli/src/util.rs:192-202`), which tan-cli#376 ported the other two guards without. alp-sdk's scripts use `@dataclass(slots=True)`, so an interpreter below the floor dies inside alp-sdk with `dataclass() got an unexpected keyword argument 'slots'` -- validator exit 1 WITH a traceback, which this command reclassifies to `validate.failed` and reports by quoting the traceback's last line. Exit 2, like guards 1 and 2, with `data.commandLine` empty: nothing ran. Two deliberate divergences from the oracle, both matching `generate_cmd`/`model_cmd`'s existing guard: the floor is the resolved SDK's declared `metadata/bootstrap.json` `pythonMinVersion` rather than a compiled-in 3.10, and the interpreter probed is `_planner_python`'s (the one that will be spawned) rather than a bare PATH `python`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.spawn-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "The `validate.` prefix is applied by the local `fail()` closure. Added by tan-cli#376. The ONE `validate` code that exits 1 (RUNTIME_FAILURE) rather than 2: the validator subprocess could not be STARTED at all (no interpreter on PATH, the script unreadable), so nothing validated anything and this is not a verdict about the board -- the exception tan-cli#262 itself carved out. A validator that started and then failed to produce a usable verdict is `validate.failed` at exit 2. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.clean",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "Assembled as `Issue(f\"validate.{result.outcome}\", ...)` from `OUTCOME_CLEAN`. Added by tan-cli#376. Counter-intuitive but real, and the oracle's own shape (`to_cli_issues` maps every parsed issue to `validate.<outcome>` before its clean-and-empty early return): `scripts/validate_board_yaml.py` renders EVERY diagnostic to stderr and only exits 1 when `collector.has_errors()`, so a board carrying warnings only exits 0 with findings to report. The run is a PASS -- exit 0, `data.outcome: \"clean\"`, `ok: true` -- and each issue under this code is a `warning` (or a `note`, for a standalone `hint:` line); it never carries an `error`. A consumer must read `exitCode`/`severity`, not the presence of an issue, to decide whether the board passed. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "Assembled as `Issue(f\"validate.{result.outcome}\", ...)` from `OUTCOME_FAILED`. Added by tan-cli#376. The validator ran but produced no usable verdict: an exit status outside the 0-3 range `_STATUS_OUTCOME` names, a crash (exit 1 WITH a Python traceback -- reclassified off `schema-violation` so a broken validator environment is never reported as a board defect, issue #38), or a run past `VALIDATOR_TIMEOUT_S`. Exit 2, NOT the oracle's exit 1 for the same outcome -- the deliberate tan-cli#262 divergence, documented at length in `validate_cmd.py`'s module docstring. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.missing-preset",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "Assembled as `Issue(f\"validate.{result.outcome}\", ...)` from `OUTCOME_MISSING_PRESET` (validator exit 2). Added by tan-cli#376. `severity` here is the outcome's own (`severity_for_outcome`: a missing preset is the one outcome whose FAIL lines are warnings, not errors); an individual issue under this code can still be `error` (the synthesized fallback) or `note` (a standalone `hint:` line), since the validator's own per-line severity wins. Today's `scripts/validate_board_yaml.py` only ever exits 0 or 1, so this outcome is unreachable against a current SDK -- it is kept because `Outcome` is the vocabulary shared with the TS/extension side. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "validate.hardware-revision",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/validate_cmd.py",
      "note": "Assembled as `Issue(f\"validate.{result.outcome}\", ...)` from `OUTCOME_HARDWARE_REVISION` (validator exit 3). Added by tan-cli#376. Same reachability note as `validate.missing-preset`: today's `scripts/validate_board_yaml.py` only ever exits 0 or 1, and this outcome is kept because it is part of the shared `Outcome` vocabulary. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.artefact-write-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build/materialise.py",
      "note": "Constructed as the whole literal at a `MaterialiseError` call site in `materialise.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.conflicting-flags",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "Constructed as the whole literal at a `_refuse` call site in `build_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.flag-retired",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "Added by tan-cli#427: `--plan`/`--manifest`/`--manifest-from` are the v0.4.1 oracle's own flags, but this port implements a newer overlapping plan surface (`--plan-from`/`--materialise`/`--native`/`--execute`) and the maintainer's decision on the issue was to retire the oracle spellings rather than carry two -- \"Two overlapping plan surfaces is worse than one, so the oracle spellings go.\" Distinct from `cli.command-deferred` (RUNTIME_FAILURE, exit 1, \"not built yet, an issue tracks its arrival\") on purpose: a retired flag never arrives, so it exits `VALIDATION_FAILURE` (2), this CLI's position for a permanently-invalid invocation (`build.conflicting-flags`'s own exit code). Constructed as the whole literal at a `_refuse` call site, same shape `build.conflicting-flags` above is registered for. Every message this code carries names the replacement flag directly (`_RETIRED_FLAGS` in `build_cmd.py`) rather than pointing at a tracker. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.materialise-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "Constructed as the whole literal at a `BuildError` call site in `build_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.path-escape",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build/materialise.py",
      "note": "Constructed as the whole literal at a `MaterialiseError` call site in `materialise.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.plan-invalid",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/core/build_plan.py",
      "note": "Constructed as the whole literal at a `PlanParseError` call site in `build_plan.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.plan-warning",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "One per entry in the build plan's own `warnings[]`, carrying that warning's OPEN-set code, its optional coreId and its message inside the text (tan-cli#1000). Registered as a single closed code on purpose: `core/build_plan.py`'s `_warnings` documents the plan-warning codes as an open set, so minting `build.<warning code>` would emit unregistered codes the moment the planner adds one. Third sibling of `build.missing-tool` (tan-cli#283/#801) and `build.cross-drive-workspace` (tan-cli#697) -- same promotion, a refusal class those two did not match. Pre-consumer: nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change."
    },
    {
      "code": "build.plan-token-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build/token_substitution.py",
      "note": "Constructed as the whole literal at a `TokenSubstitutionError` call site in `token_substitution.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.plan-unavailable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build_cmd.py",
      "note": "Constructed as the whole literal at a `BuildError` call site in `build_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.plan-unsupported-schema",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/core/build_plan.py",
      "note": "Constructed as the whole literal at a `PlanParseError` call site in `build_plan.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.project-root-mismatch",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build/token_substitution.py",
      "note": "Constructed as the whole literal at a `TokenSubstitutionError` call site in `token_substitution.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.sdk-commit-mismatch",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build/token_substitution.py",
      "note": "Constructed as the whole literal at a `TokenSubstitutionError` call site in `token_substitution.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "build.sdk-root-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/build/token_substitution.py",
      "note": "Constructed as the whole literal at a `TokenSubstitutionError` call site in `token_substitution.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "explain.ambiguous-selector",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/explain_cmd.py",
      "note": "Constructed as the whole literal at a `ExplainError` call site in `explain_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "explain.catalog-unreadable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/explain_cmd.py",
      "note": "`tan explain --code` resolved an alp-sdk checkout but its `metadata/error-catalog.json` is absent, unreadable, not JSON, or wrong-shaped. DISTINCT from `explain.sdk-root-unresolved` on purpose: the checkout is fine and the message names the missing FILE plus the generator that rebuilds it (`scripts/gen_error_catalog.py`), and the envelope still carries the `sdk` block that answered. Constructed as the whole literal at an `ExplainError` call site (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches it yet, so renaming or dropping it is not a breaking wire change."
    },
    {
      "code": "explain.code-unknown",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/explain_cmd.py",
      "note": "`tan explain --code` read the catalogue but the code is not in it. Sibling of `explain.template-unknown`/`explain.target-unknown`, one per selector kind. `data.suggestions` carries the same difflib shortlist the text line names, so a consumer gets the recovery options structured rather than having to re-parse a sentence; it is `[]`, not absent, when nothing was close. Constructed as the whole literal at an `ExplainError` call site. Pre-consumer."
    },
    {
      "code": "explain.sdk-root-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/explain_cmd.py",
      "note": "`tan explain --code` with no alp-sdk checkout resolvable by any ladder tier. Severity `error` and exit 1, NOT `presets.sdk-root-unresolved`'s warning-plus-empty-lists: `presets` still has useful built-in defaults to return without a checkout, whereas a diagnostic code has exactly one source and answering from a vendored copy would answer confidently out of date (I-26/ADR-0017). The `sdk` key is ABSENT on this envelope -- nothing was resolved, so nothing is claimed. EMISSION CONDITION, as of tan-cli#627: fires both when the ladder's `resolve_sdk_root_ladder` returns no path at all (every tier empty) AND when its terminal `--sdk-root` tier returns a path with no `tan.core.shapes.SDK_MARKER` under it -- `resolve_sdk_root_ladder` itself does not check the marker on that tier (I-31: a typo'd flag must not fall through to a lower tier), so `bind_sdk` applies the check `--code` needs before treating the flag's target as a real checkout; without it an unmarked `--sdk-root` reached `resolve_code` and misreported as `explain.catalog-unreadable` (naming a generator to run inside a directory that was never an alp-sdk checkout). Both branches share this one code and message; the caller cannot distinguish 'no tier answered' from 'the flag answered wrong' from the envelope alone, which is acceptable because the remedy (`--sdk-root <path>` to a real checkout) is the same either way. Constructed as the whole literal at an `ExplainError` call site. Pre-consumer."
    },
    {
      "code": "explain.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/explain_cmd.py",
      "note": "Constructed as the whole literal at a `ExplainError` call site in `explain_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "explain.positional-template-conflict",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/explain_cmd.py",
      "note": "Constructed as the whole literal at a `ExplainError` call site in `explain_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "explain.target-unknown",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/explain_cmd.py",
      "note": "Constructed as the whole literal at a `ExplainError` call site in `explain_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "explain.template-unknown",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/explain_cmd.py",
      "note": "Constructed as the whole literal at a `ExplainError` call site in `explain_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "explain.template-unreadable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/explain_cmd.py",
      "note": "Constructed as the whole literal at a `ExplainError` call site in `explain_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.manifest-invalid",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Constructed as the whole literal at a `_error` call site in `flash_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "flash.manifest-not-found",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/flash_cmd.py",
      "note": "Constructed as the whole literal at a `_error` call site in `flash_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.board-sku-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Constructed as the whole literal at a `GenerateError` call site in `generate_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.board-yaml-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Constructed as the whole literal at a `GenerateError` call site in `generate_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Constructed as the whole literal at a `GenerateError` call site in `generate_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.invalid-executor",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Constructed as the whole literal at a `GenerateError` call site in `generate_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.invalid-target",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Constructed as the whole literal at a `GenerateError` call site in `generate_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.output-unwritable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Constructed as the whole literal at a `GenerateError` call site in `generate_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.overlay-not-owned",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "tan-cli#501: a non-fatal `Issue`, not a `GenerateError` -- `boards/native_sim_native_64.overlay` already exists and lacks tan's own banner, but `native-sim-overlay` reached `targets` only via the bare/`--all` default set rather than an explicit `--target`, so the run drops that one target and continues (exit 0) instead of refusing the other eight the way an explicit ask still does (`generate.would-overwrite`). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.python-too-old",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Constructed as the whole literal at a `GenerateError` call site in `generate_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.sdk-root-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Constructed as the whole literal at a `GenerateError` call site in `generate_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.would-overwrite",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Constructed as the whole literal at a `GenerateError` call site in `generate_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "generate.write-escapes-project",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/generate_cmd.py",
      "note": "Raised as a `GenerateError` with `ExitCode.WRITE_FAILURE` when `resolve_confined` finds a target's output path resolving outside the project root, refusing the whole run rather than any target. Added by tan-cli#325 (`fix(init,generate): confine writes to the project after symlink resolution`) and caught UNREGISTERED by the tan-cli#224 emit-site gate the first time the two met in one tree -- the gate's first catch on code it did not itself ship. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "image.manifest-invalid",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/image_cmd.py",
      "note": "Constructed as the whole literal at a `_error_outcome` call site in `image_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "image.manifest-unavailable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/image_cmd.py",
      "note": "Constructed as the whole literal at a `_error_outcome` call site in `image_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.board-yaml-unreadable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.board-yaml-unsupported",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.example-not-found",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.example-unreadable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.invalid-cores",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.invalid-example",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.invalid-name",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.invalid-som",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.invalid-topology",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "tan-cli#996 (alp-sdk#1652's `--cores` scaffold selector, ported as `tan init --topology`). EMISSION CONDITION: `--topology`'s value fails `tan.core.example_catalog.parse_topology_arg` -- an entry with no `:`, an empty core id or os, or a core id repeated more than once. Raised before any SDK checkout is even resolved (the parse is pure string handling), so this fires the same whether or not `--sdk-root` was given. Constructed as the whole literal at an `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.invalid-template",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.scaffold-input-conflict",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "tan-cli#996, extended tan-cli#1001 review. `--template`, `--from-example`, and `--topology` are three alternative ways to say which project to scaffold; more than one given at once is refused rather than silently preferring one (the same posture `init.topology-ambiguous` takes when a topology itself is ambiguous). EMISSION CONDITION (two independent sites, same code -- distinguished only by message text): (1) more than one of the three flags above is non-null, message names every flag that WAS given (not just the first two), e.g. \"got --topology, --template\"; (2) `--topology` and `--cores` are both non-null -- `--topology` already selects the template by its full hardware topology, so `--cores` (which splices a companion core onto an ALREADY-CHOSEN template) has nothing left to attach to. Before site (2) existed, `--topology` + `--cores` silently discarded `--cores`: `ok: true`, exit 0, `issues: []`, no trace of the requested core in the written `board.yaml` -- measured, tan-cli#1001 review. `--from-example` + `--cores` is a different, pre-existing, NOT-refused case (the example ships its own board.yaml, so `--cores` is silently ignored there, documented in `init_cmd.py` at the `_plan_from_example` call site) -- out of scope for this code. Constructed as the whole literal at two `InitError` call sites in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.sdk-root-invalid",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "tan-cli#642. EMISSION CONDITION: an explicit `--sdk-root` was given AND the path it resolves to has no `scripts/alp_project.py` under it (`_is_sdk_checkout`) -- a typo'd path, or (the more realistic route) a previously-valid `--sdk-root` that `tan bootstrap` has since relocated. Fires on the TEMPLATE path only (`_sdk_root_flag_unresolved_issue`, called from `init()` alongside the other two SDK-resolution warnings); the `--from-example` path already hard-refuses the identical condition as `init.sdk-root-unresolved` (error, exit 2) before this could ever run, because an SDK checkout is not optional there. Severity `warning`, `ok:true`, `exitCode:0` -- the project still scaffolds; only `.alp/sdk-path` is not written, mirroring `_pin_sdk`'s own silent-skip for an invalid checkout. Before this code existed, `--sdk-root <bad path>` on the template path produced `ok:true`, `issues:[]`, `sdkPinned:null`, and no `.alp/sdk-path` -- indistinguishable on the wire from the ordinary case of no `--sdk-root` given at all, even though the README promises `init` pins the checkout in `.alp/sdk-path`. A LATER command then silently fell through to `~/.alp/sdk-default` -- some other project's last `bootstrap`, in a shared or multi-project setup. Constructed as the whole literal at a direct `Issue(...)` call (not an `InitError`, since this is a warning appended to a successful outcome, not a raised failure) in `init_cmd.py` (`_FULL_CODE_CALLABLES` need not cover this one -- it is a shape-1 literal site). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.sdk-root-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.som-unsupported",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "tan-cli#579. `--som` names a SoM family tan vendors no scaffold tree for (E1M-NX9* today), so a family-split template cannot be rendered for it. Before this code existed, `_family_bucket`'s `else` arm sent every such SKU onto the ALIF tree and `tan init --som E1M-NX9101 --template sensor-starter` wrote Alif content -- `preset: e1m-evk`, `chips: [tmp112]`, an `alp_e1m_aen801_m55_hp/...` README build line, and a CMakeLists.txt pinned to `--core m55_hp` next to a board.yaml declaring `m33` -- at ok:true/exit 0/issues:[]. Distinct from `init.invalid-som`, which means \"this TEMPLATE supports one SKU\" (iot-starter); this one means \"tan has no scaffold for this SoM family in ANY family-split template\". Constructed as the whole literal at an `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it. RETIRES when alp-sdk's scaffold catalog gains an NXP entry and the tree is vendored: fill in the `None` in `tan/core/scaffold.py`'s `_SOM_FAMILIES` and this stops firing."
    },
    {
      "code": "init.som-flow-style-unsupported",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "tan-cli#1029, `Refs #1008`. `--from-example <ex> --som <sku>` (and `--topology ... --som <sku>`, which delegates to the same path) onto a board.yaml whose `som:` block is written in YAML FLOW style (`som: {sku: ..., hw_rev: ...}`, valid YAML but on one physical line) used to silently discard `--som`: `tan.core.scaffold.retarget_board_yaml_som` -- a deliberately-not-a-YAML-parser line-oriented scanner, the same one tan-cli#1008 spent six review rounds hardening for BLOCK-style `som:` shapes (a trailing comment, trailing whitespace, quoted scalars, `som :`, `hw_rev :`, `sku :`) -- never recognised a flow mapping's content at all, so it returned the example's board.yaml byte-for-byte unchanged while `tan.core.scaffold.vendored_som` (the reader `init`'s own `init.hw-rev-not-buildable` check also uses) reported `(None, None)` for it: `ok:true`, `exitCode:0`, `issues:[]`, and the scaffolded board.yaml still names the EXAMPLE's original SoM, not the one `--som` asked for. Unlike tan-cli#743/#1008, the file stays self-consistent either way (the SAME `vendored_som` blindness `tan validate` would hit too, so no init/validate contradiction) -- this is a silently-discarded argument, not a contradiction between two commands. `FlowStyleSomError` (`tan.core.scaffold`) is raised by `vendored_som` the instant it sees this shape and inherited by `retarget_board_yaml_som`, which calls it first -- one shared rule for both reader and writer, the same pattern `top_level_key_name`/`_is_som_key_line`/`_split_child_key` already hold. Refusing loudly was chosen over teaching the scanner to parse flow style: doing so safely needs a real YAML parser (telling a comma inside a quoted value apart from one separating two keys, an escaped brace from a real one), not a seventh line-oriented special case on top of tan-cli#1008's own six-round history of exactly that growth pattern -- and reachability is nil: 0 of the SDK's 100 tracked `board.yaml` files and 0 vendored templates use flow style, hand-authored only. `--from-example`/`--topology` with NO `--som` at all (flow style copied through unretargeted, since nothing was asked to change) is unaffected -- still `ok:true`; `init.hw-rev-not-buildable`'s own advisory read of the same shape degrades to \"cannot judge\" (no issue), the same graceful skip an unparsed `--board-yaml` already gets. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.som-block-unsupported",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "tan-cli#1041 (the amendment: the issue widened from three named spellings to six). `--from-example <ex> --som <sku>` (and `--topology ... --som <sku>`, same delegated path) onto a board.yaml whose `som:` block is a YAML ALIAS (`som: *base`), a `<<:` merge key -- inside the `som:` block or at the document root -- with no explicit `sku:` override, or a flow mapping split across MORE than one physical line (`som:\\n  {sku: ..., hw_rev: ...}`, with or without a leading comment or an anchor on the `som:` line) used to silently discard `--som` the identical way `init.som-flow-style-unsupported` (tan-cli#1029) already covers for a SAME-line flow mapping: `retarget_board_yaml_som` found no literal `sku:` line to rewrite and returned the example's board.yaml byte-for-byte unchanged, `ok:true`/`exitCode:0`/`issues:[]`, still naming the example's original SoM. A DIFFERENT code from `init.som-flow-style-unsupported`, deliberately, even though the remediation text is close to identical (rewrite the `som:` block in block style) -- the diagnosis is not: a consumer that ever wants to tell 'your `som:` is flow style' apart from 'your `som:` uses a YAML feature (alias/merge) this scaffolder does not follow' needs two codes, not one broadened past its own name. `UnreadableSomBlockError` (`tan.core.scaffold`) is raised by `vendored_som` -- either because its own line scan matched a literal `som:` line but found no `sku:` child beneath it, or (the document-root-merge case, where no `som:` TEXT exists anywhere for a line scan to find at all) because a `yaml.safe_load` backstop resolved an effective `som:` mapping the scan never saw coming -- and inherited by `retarget_board_yaml_som`, which calls `vendored_som` first, same pattern as `FlowStyleSomError`. Reachability: 0 of the SDK's 100 tracked `board.yaml` files use a `som:` alias, a merge key, or a multi-line flow mapping; 0 of the 10 vendored templates -- hand-authored only, same as its sibling code. `--from-example`/`--topology` with NO `--som` at all is unaffected -- still `ok:true`; `init.hw-rev-not-buildable`'s own advisory read of the same shape degrades to \"cannot judge\" (no issue), the same graceful skip `init.som-flow-style-unsupported` and an unparsed `--board-yaml` already get. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.template-unreadable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "Constructed as the whole literal at a `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.topology-ambiguous",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "tan-cli#996 (alp-sdk#1652's `AmbiguousCoresError`, ported into `tan.core.example_catalog.find_example_by_cores` as `AmbiguousCoresTopologyError`). EMISSION CONDITION: `--topology`'s exact core/os topology matches MORE THAN ONE record in the SDK's live `metadata/templates/catalog-v1.json`. The message names every matching template id, sorted, never just the first -- the whole point of this code existing is that a customer chooses, rather than tan guessing on their behalf and hiding the other candidates. Not a hypothetical: measured against the alp-sdk v0.16.0 catalog, the single-core `{\"m55_hp\": \"zephyr\"}` topology alone already matches five templates (`diagnostics`, `iot`, `minimal`, `peripheral`, `sensor`) -- see `tests/planner/test_find_template_by_cores.py::test_the_live_catalog_is_ambiguous_for_a_single_m55_hp_zephyr_topology`. Constructed as the whole literal at an `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "init.topology-not-found",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/init_cmd.py",
      "note": "tan-cli#996 (alp-sdk#1652's own `find_template_by_cores` raises `TemplateNotFoundError` for the same zero-match case; the standalone `find_example_by_cores` this code's caller uses raises `CoresTopologyNotFoundError` instead). EMISSION CONDITION: `--topology`'s exact core/os topology matches NO record in the bound SDK's live catalog. The message names every topology the catalog DOES offer, so the customer can see what is actually on the menu rather than guessing again blind. Constructed as the whole literal at an `InitError` call site in `init_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "kconfig.board-yaml-invalid",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/kconfig_cmd.py",
      "note": "Constructed as the whole literal at a `_CoreResolutionError` call site in `kconfig_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "kconfig.board-yaml-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/kconfig_cmd.py",
      "note": "Constructed as the whole literal at a `_CoreResolutionError` call site in `kconfig_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "kconfig.core-ambiguous",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/kconfig_cmd.py",
      "note": "Constructed as the whole literal at a `_CoreResolutionError` call site in `kconfig_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.board-yaml-invalid",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "Constructed as the whole literal at a `ModelError` call site in `model_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.board-yaml-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "Constructed as the whole literal at a `ModelError` call site in `model_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.sdk-root-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "Constructed as the whole literal at a `ModelError` call site in `model_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.doctor-sdk-unresolved",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "tan-cli#781: `model doctor`'s own SDK-unresolved case, distinct from `model.sdk-root-unresolved` above -- `doctor` folds an unresolved/rejected `--sdk-root` into a WARNING (the backend-availability rows it reports do not read `metadata/**` at all and are unaffected), where `build` raises a `ModelError` at `error` severity and refuses outright for the same input. Reusing `model.sdk-root-unresolved` at a different severity across the two subcommands would make one code mean two different things on the wire; this is a separate literal instead. A plain `Issue(\"model.doctor-sdk-unresolved\", ...)` construction in `_run_doctor`, not a `ModelError` (`doctor` never raises for this). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.check-sku-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "tan-cli#782: `model check`'s board-level SKU refusal -- `resolve_check_backends` (`tan.model.check`) raised `FileNotFoundError`/`ValueError` resolving `som.sku`'s real NPU backends (no SoM preset, a malformed `silicon:` ref, or no SoC spec on disk), reported once for the whole run via a `ModelError` rather than once per declared model. Constructed as the whole literal at a `ModelError` call site in `model_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.check-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "tan-cli#782: `model check`'s per-model failure -- an unreadable/unparseable declared model source, most commonly -- caught around `check_model_backends()` and reported as a coded issue rather than a traceback, mirroring `model.build-failed`'s exact shape so one bad model in a multi-model board.yaml does not abort the batch. A plain `Issue(\"model.check-failed\", ...)` construction in `_check_one_model`, not a `ModelError` (the run continues to the next model). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.target-caveat",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "tan-cli#789 review (f): one per caveated target in a package `model build` just wrote -- the COMPILER's own unresolved caveat about the blob being shipped, carried verbatim from `tan.model.adapters.Blob.caveats` through the `.alpmodel` manifest's per-target `caveats` list and read back out of the written file by `_shipped_caveat_issues` (`package.read_manifest_file`). Today that is vela's \"Compilation may be invalid or non-optimal\" verdict when it fell back to its BUILT-IN default profile, whose memory model may not be the module's -- which matters because the `arena`/`requires.sram_kib` figures beside it in the same manifest entry are what alp-sdk's on-device selector gates fit on (`src/backends/inference/alp_model_select.c`). A WARNING, never an error: the package is written and valid, and `_run_build`'s exit code keys on error-severity issues only. A plain `Issue(\"model.target-caveat\", ...)` construction in `_shipped_caveat_issues`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "model.caveat-readback-failed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/model_cmd.py",
      "note": "tan-cli#789 review (f): `model build` wrote a package but could not decode its manifest to report the compiler caveats it ships with (`package.read_manifest_file` raised). Reported rather than swallowed because silence here is indistinguishable from `model.target-caveat`'s absence, i.e. from \"this package has no caveats\" -- the exact ambiguity the caveat field exists to end. A WARNING, not an error: the artifact IS on disk, and downgrading a successful build to a failure over a diagnostic read would be the wrong trade. A plain `Issue(\"model.caveat-readback-failed\", ...)` construction in `_shipped_caveat_issues`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "monitor.launch-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/monitor_cmd.py",
      "note": "Constructed as the whole literal at a `MonitorError` call site in `monitor_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "monitor.no-port",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/monitor_cmd.py",
      "note": "Constructed as the whole literal at a `MonitorError` call site in `monitor_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "monitor.pyserial-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/monitor_cmd.py",
      "note": "Constructed as the whole literal at a `MonitorError` call site in `monitor_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "size.manifest-invalid",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/size_cmd.py",
      "note": "Constructed as the whole literal at a `_error_outcome` call site in `size_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "size.manifest-unavailable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/size_cmd.py",
      "note": "Constructed as the whole literal at a `_error_outcome` call site in `size_cmd.py` (`_FULL_CODE_CALLABLES` in python/tests/gates/test_every_issue_code_is_registered.py), several call frames from the eventual `Issue(err.code, ...)` re-emit -- invisible to a scan scoped to `Issue(\"family.code\", ...)`/`code=\"family.code\"` alone. Found and registered while remediating tan-cli#224's own review (the gate's first version missed this whole emit shape). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.board-yaml",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.bootstrap-manifest",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.home-path",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.host-prerequisites",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.host-python",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.jlink",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.libraries",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The ADR-0018 curated-library verdict for the selected project (tier + licence + whether each selection can be wired on this target), ported out of alp-sdk's `scripts/alp_cli/doctor.py::_check_libraries` under ADR-0020 end-state B -- a user-facing check belongs on the user command surface, which is `tan`. `severity` is `warning` and only ever `warning`: `libraries_check` never returns a `fail` status, because `tan build` already refuses an unwireable selection outright and a `fail` here would additionally cost exit 4. Emitted only when a project selects at least one curated library; a project selecting none produces no check and therefore no issue at all. The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.toolchain",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "issue #474 (ADR 0021 Lane 1 P1): `toolchain_check` -- stamp-vs-pin, never directory-exists (\"a stamped 1.0.1 store against a moved pin is a Fail with a fix, not 'a toolchain exists'\", the ADR's own words). `severity` is always `error`: the check only ever returns `unknown` (no `issues[]` entry at all, per `checks_to_issues()`) or `fail`, never `warn` -- no verified `arm-zephyr-eabi` toolchain for the checkout's pinned Zephyr SDK version means `tan build` for real silicon cannot succeed. Distinct from `doctor.zephyr-sdk` (unconditional, no SDK checkout needed, answers whether ANY toolchain exists on the host); this one needs `sdk_root` and answers whether THIS checkout's PIN is satisfied, verified. Calls `tan.core.toolchain_provision.stamp_matches_pin` -- the SAME function `tan bootstrap`'s own skip-if-already-installed step calls -- so doctor and bootstrap cannot independently drift on what \"still valid\" means. The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.long-paths",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.python-floor",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.sdk",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.sdk-provenance",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.setools",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.seven-zip",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.venv-provenance",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.west",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.west-resolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.workspace",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.zephyr-sdk",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.zephyr-sdk-available-for-host",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.zephyr-version",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.zephyr-workspace",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "The `doctor.` prefix is applied by `checks_to_issues()`'s `check.code or f\"doctor.{kebab_check_name(check.name)}\"` -- kebab-cased via `kebab_check_name` since the camelCase-issue-code fix. The Rust oracle's own `doctor.<checkName>` convention still emits `c.name` raw (`format!(\"doctor.{}\", c.name)`, `doctor.rs`), so this is now a deliberate, documented Python-only divergence from the oracle, not the verbatim mirror it used to be. Resolved out of an `_ACKNOWLEDGED_CEILINGS` entry into `_RESOLVABLE_HELPERS` while remediating tan-cli#224's own review: the ceiling's stated cost (\"a materially bigger audit\") did not survive measurement -- 48 `Check(...)` call sites, every one literal. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "migrate.failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/west_forward_cmd.py",
      "note": "The mirrored `f\"{subcommand}.failed\"` shape (family substituted, suffix fixed) at `python/tan/commands/west_forward_cmd.py`'s `_run_forward` -- the reverse of `bootstrap.`-style prefixing, and a second live call site of the exact defect tan-cli#224 reports (a code assembled by interpolation no literal scan can see) found while remediating that issue's own review. `subcommand` is closed to the three literal strings `tan migrate`'s own Typer command passes (`migrate`/`lock`/`quality`, `cli.py`). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "lock.failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/west_forward_cmd.py",
      "note": "The mirrored `f\"{subcommand}.failed\"` shape (family substituted, suffix fixed) at `python/tan/commands/west_forward_cmd.py`'s `_run_forward` -- the reverse of `bootstrap.`-style prefixing, and a second live call site of the exact defect tan-cli#224 reports (a code assembled by interpolation no literal scan can see) found while remediating that issue's own review. `subcommand` is closed to the three literal strings `tan lock`'s own Typer command passes (`migrate`/`lock`/`quality`, `cli.py`). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "quality.failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/west_forward_cmd.py",
      "note": "The mirrored `f\"{subcommand}.failed\"` shape (family substituted, suffix fixed) at `python/tan/commands/west_forward_cmd.py`'s `_run_forward` -- the reverse of `bootstrap.`-style prefixing, and a second live call site of the exact defect tan-cli#224 reports (a code assembled by interpolation no literal scan can see) found while remediating that issue's own review. `subcommand` is closed to the three literal strings `tan quality`'s own Typer command passes (`migrate`/`lock`/`quality`, `cli.py`). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "quality.profile-required",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/west_forward_cmd.py",
      "note": "NEW, tan-cli#454: `scripts/west_commands/alp_quality.py` declares `--profile {quick,pr,full,release}` REQUIRED, with no default tan could pick on a caller's behalf, and this flag was never declared on tan's own Typer surface -- every realistic `tan quality` without it reached `west` and died on the CHILD's own argparse usage error, forwarded back opaquely as `quality.failed`, indistinguishable from `west` itself failing, and undiscoverable from `tan quality --help`. `_refuse_required` in `west_forward_cmd.py` now catches the omission BEFORE `west` is ever spawned and reports this code instead. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "migrate.mode-required",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/west_forward_cmd.py",
      "note": "NEW, tan-cli#454: `scripts/west_commands/alp_migrate.py` declares a REQUIRED mutually-exclusive `(--check | --preview | --apply)` group, with no default tan could pick on a caller's behalf (reporting drift, previewing a diff and rewriting board.yaml are not interchangeable), and none of the three was ever declared on tan's own Typer surface -- every realistic `tan migrate` without one reached `west` and died on the CHILD's own argparse usage error, forwarded back opaquely as `migrate.failed`, indistinguishable from `west` itself failing, and undiscoverable from `tan migrate --help`. `_refuse_required` in `west_forward_cmd.py` now catches the omission BEFORE `west` is ever spawned and reports this code instead. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "diff.board-yaml-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/diff_cmd.py",
      "note": "Assembled by `_emit_failure`'s `f\"diff.{code}\"` (prefix template) when diff's board.yaml path does not resolve or the file does not exist. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "diff.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/diff_cmd.py",
      "note": "Assembled by `_emit_failure`'s `f\"diff.{code}\"` (prefix template): board.yaml could not be read (OSError/UnicodeDecodeError), or diff's outer backstop `except Exception` caught something unexpected. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "diff.pyyaml-unavailable",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/diff_cmd.py",
      "note": "_load_document refuses when PyYAML is not installed in this environment -- diff cannot even parse board.yaml without it. Reaches the wire as `diff.pyyaml-unavailable` via ParseFailure.code forwarded through _emit_failure's `f\"diff.{code}\"` prefix template. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "diff.schema-violation",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/diff_cmd.py",
      "note": "board.yaml is not valid YAML, or a field normalize_board_model needs is the wrong shape (a live false-refusal exists here for YAML-1.1-only bool spellings PyYAML's SafeLoader has already collapsed -- see the module docstring). Also the outcome `_reject_if_sdk_validator_disagrees` (tan-cli#455) raises when the resolved SDK's own `validate_board_yaml.py` rejects the same board.yaml with a real schema violation -- see `diff.failed`/`diff.missing-preset`/`diff.hardware-revision` for that cross-check's other possible outcomes. Reaches the wire as `diff.schema-violation` via ParseFailure.code forwarded through _emit_failure's `f\"diff.{code}\"` prefix template. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "diff.python-too-old",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/diff_cmd.py",
      "note": "tan-cli#455 review round: `_reject_if_sdk_validator_disagrees`'s own guard 3, ported from `validate_cmd`'s (`resolve_manifest_python_floor` + `_python_too_old`) -- refuses BEFORE spawning the SDK's `validate_board_yaml.py` when the resolved interpreter is below the SDK's declared `pythonMinVersion`, rather than let that spawn crash with a `dataclass() got an unexpected keyword argument 'slots'` traceback the outcome-mapping below would otherwise have to reclassify. Reaches the wire via ParseFailure.code forwarded through _emit_failure's `f\"diff.{code}\"` prefix template. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "diff.spawn-failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/diff_cmd.py",
      "note": "tan-cli#455 review round: the SDK validator subprocess could not even be started (no interpreter on PATH, the script unreadable) -- nothing validated anything, so a clean diff must not be reported either. Mirrors `validate.spawn-failed`'s own RUNTIME_FAILURE (exit 1) split from every other outcome; this module used to swallow the same failure silently via a blanket `except (OSError, ValueError, subprocess.SubprocessError): return`. Reaches the wire via ParseFailure.code forwarded through _emit_failure's `f\"diff.{code}\"` prefix template. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "diff.failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/diff_cmd.py",
      "note": "tan-cli#455 review round: the SDK validator ran but produced no usable verdict -- a crash (exit 1 WITH a Python traceback, reclassified off `schema-violation` by `analyze_validator_output` so a broken validator ENVIRONMENT is never reported as a board defect) or a run past `VALIDATOR_TIMEOUT_S` (300s). This module used to flatten both into `diff.schema-violation`, misreporting an environment failure as an invalid board.yaml; `_reject_if_sdk_validator_disagrees` now raises `ParseFailure(result.outcome, ...)` so the real outcome reaches the wire. Reaches the wire via ParseFailure.code forwarded through _emit_failure's `f\"diff.{code}\"` prefix template. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "diff.missing-preset",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/diff_cmd.py",
      "note": "tan-cli#455 review round: same outcome-vocabulary parity as `validate.missing-preset` (validator exit 2) -- today's `scripts/validate_board_yaml.py` only ever exits 0 or 1, so this is unreachable against a current SDK and kept only because `diff` now reuses the same `analyze_validator_output`/`_STATUS_OUTCOME` machinery `validate` does. Reaches the wire via ParseFailure.code forwarded through _emit_failure's `f\"diff.{code}\"` prefix template. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "diff.hardware-revision",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/diff_cmd.py",
      "note": "tan-cli#455 review round: same outcome-vocabulary parity as `validate.hardware-revision` (validator exit 3) -- unreachable against today's `scripts/validate_board_yaml.py` (only ever exits 0 or 1), kept for the same shared-vocabulary reason `diff.missing-preset` is. Reaches the wire via ParseFailure.code forwarded through _emit_failure's `f\"diff.{code}\"` prefix template. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "pinmux.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/pinmux_cmd.py",
      "note": "The catch-all exception handler in pinmux(): any unexpected failure resolving or reading the pinmux capability table. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "pinmux.family-invalid",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/pinmux_cmd.py",
      "note": "tan-cli#359: `--family` was joined onto the SDK root unvalidated, so an absolute value discarded the prefix (`Path(sdk) / \"/other/metadata/pinmux/aen\"` IS the other path) and `..` walked out of it -- the envelope still reported the sdkRoot it never read from. Emitted for BOTH halves of the fix: a `--family` that is not a plain stem (rejected before any IO), and a resolved table path that leaves `<sdkRoot>/metadata/pinmux` anyway (a symlink inside the SDK, which the stem check structurally cannot see). Exit code 2 (VALIDATION_FAILURE), and the target is never read. The Rust oracle has no equivalent check -- a deliberate divergence, not a port gap. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "trace.sdk-root-unresolved",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/trace_cmd.py",
      "note": "Assembled by the nested fail() helper's `f\"trace.{code}\"` (prefix template) inside trace's Typer command: alp-sdk root is unresolved (no --sdk-root, no pin, no discoverable checkout). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "trace.board-yaml-missing",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/trace_cmd.py",
      "note": "Assembled by the nested fail() helper's `f\"trace.{code}\"` (prefix template): board.yaml path could not be resolved or the file does not exist. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "trace.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/trace_cmd.py",
      "note": "Assembled by the nested fail() helper's `f\"trace.{code}\"` (prefix template) when resolve_trace_targets raises TraceTargetError. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.board-yaml",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): the DEBUG-focused report's own `boardYaml` check (tan-cli#357), built by `_board_yaml_check` in support_bundle_cmd.py directly -- reports the resolved board.yaml PATH as its detail, unlike the build-checklist's `doctor.board-yaml`, which reports \"board.yaml found\" instead. `_doctor_section`, the function an earlier version of this note cited, was deleted by the #357 diff (tan-cli#374 finding 4). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.bootstrap-manifest",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): one of `_HOST_CHECK_ORDER`'s five names, harvested BY NAME from doctor_cmd._collect(...)'s own Check objects by `_host_checks_from_doctor` (tan-cli#357) -- mirrors `doctor.bootstrap-manifest` verbatim. `_doctor_section`, the function an earlier version of this note cited, was deleted by the #357 diff (tan-cli#374 finding 4). tan-cli#374 finding 5: the oracle has no `bootstrapManifest` CHECK at all -- it folds a rejected metadata/bootstrap.json into `hostPrerequisites`'s own detail instead -- so this code is a documented port-only divergence with no oracle counterpart, always `warn` (never `fail`). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.gdb",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): the DEBUG-focused report's `yocto-userspace` tool check (tan-cli#357), built by `_target_checks` in support_bundle_cmd.py -- `warn` when no local gdb/arm-none-eabi-gdb is on PATH. tan-cli#374 finding 3: reachable and emitted on the wire before this code existed in the registry, missed because the gate's own declared value space (_FORWARDER_SUFFIXES) still described the pre-#357 build checklist. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.gdbserver-backend",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): `_target_checks`'s `f\"{server}Backend\"` (tan-cli#357) for server == \"gdbserver\". NOT reachable through today's target/server pairing (debug_launch._SERVER_CHOICES only pairs gdbserver with yocto-userspace, which uses the separate `gdb` check instead), but `server` is a plain str parameter with nothing narrower enforcing that at this call site -- registered now (tan-cli#374 finding 3) so a future widening of that pairing table cannot put an unregistered code on the wire with nothing here to catch it. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.home-path",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): one of `_HOST_CHECK_ORDER`'s five names, harvested BY NAME from doctor_cmd._collect(...)'s own Check objects by `_host_checks_from_doctor` (tan-cli#357) -- mirrors `doctor.home-path` verbatim; see the sibling doctor.* entry for what this check verifies. `_doctor_section`, the function an earlier version of this note cited, was deleted by the #357 diff (tan-cli#374 finding 4). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.host-prerequisites",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): one of `_HOST_CHECK_ORDER`'s five names, harvested BY NAME from doctor_cmd._collect(...)'s own Check objects by `_host_checks_from_doctor` (tan-cli#357) -- mirrors `doctor.host-prerequisites` verbatim; see the sibling doctor.* entry for what this check verifies. `_doctor_section`, the function an earlier version of this note cited, was deleted by the #357 diff (tan-cli#374 finding 4). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.jlink-backend",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): `_target_checks`'s `f\"{server}Backend\"` (tan-cli#357) for server == \"jlink\", when --target-kind zephyr-mcu/baremetal-mcu --server jlink -- `warn` when no JLinkGDBServerCL/JLinkGDBServer is on PATH. tan-cli#374 finding 3: reachable and emitted on the wire before this code existed in the registry, missed because the gate's own declared value space (_FORWARDER_SUFFIXES) still described the pre-#357 build checklist. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.long-paths",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): one of `_HOST_CHECK_ORDER`'s five names, harvested BY NAME from doctor_cmd._collect(...)'s own Check objects by `_host_checks_from_doctor` (tan-cli#357). tan-cli#306 widened doctor_cmd.long_paths_check with a Windows-only `fail` arm the oracle cannot reach at all (its own long_paths_check takes only the registry axis and never returns worse than warn); tan-cli#374 finding 1 found that arm reaching THIS command's verdict made a first-run customer host (registry LongPathsEnabled on, no global .gitconfig) exit 4 where the oracle exits 0. `support_bundle_cmd._demote_long_paths_fail` now caps it at `warn` before it is ever harvested here, so `error` severity is structurally unreachable from support-bundle (unlike from `tan doctor`, which keeps the real fail arm -- see the sibling doctor.long-paths entry). Severity corrected warning<-error here for that reason (tan-cli#374). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.none-backend",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): `_target_checks`'s `f\"{server}Backend\"` (tan-cli#357) for server == \"none\". NOT reachable through today's target/server pairing (debug_launch._SERVER_CHOICES only pairs none with native-host, which uses the separate `lldb` check instead, always `pass`, #131), but `server` is a plain str parameter with nothing narrower enforcing that at this call site -- registered now (tan-cli#374 finding 3) so a future widening of that pairing table cannot put an unregistered code on the wire with nothing here to catch it. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.openocd-backend",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): `_target_checks`'s `f\"{server}Backend\"` (tan-cli#357) for server == \"openocd\", when --target-kind zephyr-mcu/baremetal-mcu --server openocd -- `warn` when no openocd is on PATH. tan-cli#374 finding 3: reachable and emitted on the wire before this code existed in the registry, missed because the gate's own declared value space (_FORWARDER_SUFFIXES) still described the pre-#357 build checklist. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.pyocd-backend",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): `_target_checks`'s `f\"{server}Backend\"` (tan-cli#357) for server == \"pyocd\", when --target-kind zephyr-mcu/baremetal-mcu --server pyocd -- `warn` when no pyocd is on PATH. tan-cli#374 finding 3: reachable and emitted on the wire before this code existed in the registry, missed because the gate's own declared value space (_FORWARDER_SUFFIXES) still described the pre-#357 build checklist. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.sdk-root",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): the DEBUG-focused report's own `sdkRoot` check (tan-cli#357), built directly in `_debug_doctor_report` -- `fail` when no alp-sdk checkout resolved, mirroring the oracle's `status_pass_fail(has_sdk)`. tan-cli#374 finding 3: reachable and emitted at ERROR severity on the wire before this code existed in the registry -- the exact gap that finding names (measured: a fresh project with no resolvable SDK put this code on the wire with `pytest tests/gates/ -q` reporting all green). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.zephyr-sdk-available-for-host",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Assembled by _doctor_issues's `f\"support-bundle.{doctor_cmd.kebab_check_name(c.name)}\"` (prefix template): one of `_HOST_CHECK_ORDER`'s five names, harvested BY NAME from doctor_cmd._collect(...)'s own Check objects by `_host_checks_from_doctor` (tan-cli#357) -- mirrors `doctor.zephyr-sdk-available-for-host` verbatim; see the sibling doctor.* entry for what this check verifies. `_doctor_section`, the function an earlier version of this note cited, was deleted by the #357 diff (tan-cli#374 finding 4). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "support-bundle.redaction-skipped",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/support_bundle_cmd.py",
      "note": "Added by tan-cli#499. `_redact` scrubs the resolved home directory out of the WRITTEN bundle before the user attaches it, but it is refused wholesale when $HOME is a filesystem root -- Docker/OpenShift hand a uid with no /etc/passwd entry HOME=/, and replacing that unanchored rewrote every path separator in every string of the file (measured: exit 0, no warning, 88 `<home>` substitutions in one bundle -- of the illustrative shape `workspaceRoot: \"<home>srv<home>ci<home>work<home>...\"`, real paths elided). This code is what says the file went out UNREDACTED, in the envelope AND in text mode, so the refusal is never silent. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.fix-needs-sudo",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "ADR-0021's Tier-B refusal (tan-cli#91, MAINTAINER DECISION): --fix never spawns sudo on the customer's behalf -- fix_needs_sudo_check names the exact command and stops there rather than risking a password prompt with nowhere to go under --format json. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.fix-installed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "--fix ran a manifest install command (ADR-0021 Tier A) that needed no elevation and the child exited 0 -- fix_installed_check reports this, explicitly NOT a claim the tool is now on PATH within this same process (no same-process re-check is possible). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.fix-spawn-failed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "--fix resolved a tool's install command on PATH but starting it raised (OSError/ValueError/a non-timeout subprocess.SubprocessError) -- fix_spawn_failed_check reports this distinctly from silence, so a customer watching --fix do nothing can tell the OS refused to start it. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.fix-failed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "--fix ran a tool's install command and the child exited non-zero -- fix_failed_check is the only place a customer learns the install itself failed, rather than merely 'still missing'. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.fix-timed-out",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "A tool's install command did not finish inside FIX_INSTALL_TIMEOUT_S (300s) and was killed -- fix_timed_out_check reports this so a hang does not read as up to 5 minutes of silent terminal (text mode only prints after the whole report completes). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.fix-installer-not-found",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "tan-cli#360: --fix could not resolve the program a manifest install command starts with (`brew` on a Mac with no Homebrew, `winget` on a Windows image with no App Installer), so no repair ran for the tools that command covers -- fix_installer_not_found_check names the installer AND every tool it blocked, plus the remedy for that specific installer. Emitted ONCE PER INSTALLER, not per tool: one absent `brew` blocks every macOS prerequisite at once, and six identical 'install Homebrew' paragraphs would bury the report. Before this the path was a bare `continue`, so the least-equipped hosts -- exactly the audience --fix exists for -- got the same report back with no fix:* line at all. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "doctor.fix-suppressed",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none",
      "emittedBy": "python/tan/commands/doctor_cmd.py",
      "note": "tan-cli#91 P1, measured against the oracle: `tan doctor --fix --format json` on an unhealthy host used to be a byte-for-byte silent no-op vs. plain `tan doctor`. fix_suppressed_issue reports HONESTLY instead: --fix was requested, the can_prompt consent gate refused it, and names which condition tripped (--format json, --ci, --non-interactive, or no interactive terminal). Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "debug-config.gdbserver-address-unresolved",
      "status": "reserved",
      "severity": "info",
      "consumer": "none -- no consumer matches this code yet; registered so a future one has a stable spelling to bind to rather than inventing its own",
      "emittedBy": "python/tan/commands/debug_config_cmd.py",
      "note": "tan-cli#321 direction 1, Python-only -- crates/ predates this feature and never emits it. Fires on a yocto-userspace `tan debug-config` run (both --preview and a write) whose final miDebuggerServerAddress is still the unresolved <host>:<port> placeholder: the host and gdbserver port are a runtime property of the deployed board that no build or SDK-published metadata can ever resolve. Checked against the FINAL configuration (the fresh draft on preview, the merged written configuration on write), so a customer who already hand-filled the real address is never re-nagged. Pre-consumer -- reserved for alp-sdk-vscode to surface this notice; nothing matches it yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "scaffold.name-required",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/scaffold_cmd.py",
      "note": "--name is required and scaffold is not running interactively (or the interactive prompt was skipped) -- _need_name's ScaffoldError, re-emitted verbatim by _emit_error's `Issue(err.code, ...)`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "scaffold.cancelled",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/scaffold_cmd.py",
      "note": "The interactive scaffold flow was cancelled -- _cancelled's ScaffoldError, re-emitted verbatim by _emit_error's `Issue(err.code, ...)`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "scaffold.invalid-template",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/scaffold_cmd.py",
      "note": "An unknown --template/template id was requested -- a ScaffoldError, re-emitted verbatim by _emit_error's `Issue(err.code, ...)`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "scaffold.invalid-name",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/scaffold_cmd.py",
      "note": "The given (or interactively entered) module name does not normalize to a valid identifier -- a ScaffoldError, re-emitted verbatim by _emit_error's `Issue(err.code, ...)`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "scaffold.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none",
      "emittedBy": "python/tan/commands/scaffold_cmd.py",
      "note": "An unexpected failure during scaffold planning or writing -- a ScaffoldError, re-emitted verbatim by _emit_error's `Issue(err.code, ...)`. Pre-consumer -- nothing in alp-sdk-vscode matches this code yet, so renaming or dropping it is not a breaking wire change. Promote to `frozen` (filling in consumer/consumerEffect for real) the moment a consumer binds to it."
    },
    {
      "code": "new-som.failed",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code with `===` yet; registered so the scaffold panel has a stable spelling to bind to instead of inventing one",
      "consumerEffect": "Marks a `new-som` run as refused. Until tan-cli#399 the SAME refusals reached the consumer as `cli.parse-error` on a `command: \"cli\"` envelope, so a panel branching on `command` could not tell a rejected scaffold from a CLI usage error, and a successful scaffold was not JSON at all -- the two paths of one verb disagreed about whether stdout was an envelope.",
      "emittedBy": "python/tan/commands/new_som_cmd.py",
      "note": "ONE code for every `new-som` refusal (unresolved SDK root, bad SKU/soc-ref/family/core id, unknown board, unresolvable hw rev, preset exists without --force, unwritable output root, missing flags where prompts are unavailable), matching the spelling the frozen v0.4.1 oracle's forwarder emits for the same class. The EXIT CODE, not the code string, distinguishes them: 2 for the SDK-root preflight (mirroring crates/tan-cli/src/commands/sdk_cli.rs::run), 1 for every other refusal. Pre-consumer -- promote to `frozen` (filling in consumer for real) the moment a consumer binds to it."
    },
    {
      "code": "new-som.internal-failure",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code with `===` yet",
      "consumerEffect": "Reserved to let a consumer separate 'tan is broken' from 'your input was rejected': a generated skeleton that fails its own schema self-check is a tan bug, never bad user input, and nothing the porting engineer can fix by re-running with different flags.",
      "emittedBy": "python/tan/commands/new_som_cmd.py",
      "note": "Both in-memory self-checks (`som-preset-v1`, `soc-spec-v1`) run BEFORE anything touches the filesystem, so this code always means nothing was written. Exit 1 (RuntimeFailure), matching the sibling `*.internal-failure` codes. Added by tan-cli#399 with the rest of new-som's envelope: before it, this path exited 1 with prose on stderr and left `cli.main`'s generic fallback to invent a `cli.parse-error` envelope for it."
    },
    {
      "code": "faultdecode.no-registers",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code with `===` yet; registered so a fault-decode panel has a stable spelling to bind to",
      "consumerEffect": "Distinguishes 'nothing to analyse' from a tan crash on the one refusal `faultdecode` has. Reachable only under `--format json`: the command's own `--json` is the unwrapped SDK report surface (see faultdecode_cmd.py's module docstring) and keeps writing the human error line to stderr, envelope-free, exactly as the forwarded child did.",
      "emittedBy": "python/tan/commands/faultdecode_cmd.py",
      "note": "Exit 2 (ValidationFailure), matching the `click.UsageError` the alp_cli original raised for the same input. Added by tan-cli#399, which made `--format json` emit tan's envelope with the decode report as `data`; without this the refusal path went out as `command: \"cli\"` / `cli.parse-error` while the success path emitted `command: \"faultdecode\"`. Pre-consumer -- promote to `frozen` (filling in consumer for real) the moment a consumer binds to it."
    },
    {
      "code": "faultdecode.invalid-register-value",
      "status": "reserved",
      "severity": "error",
      "consumer": "none -- no consumer matches this code with `===` yet; registered so a fault-decode panel has a stable spelling to bind to",
      "consumerEffect": "Separates 'that register value cannot be read' from both a successful decode and `faultdecode.no-registers`: the input was supplied, it was just not a register value, so re-running with the same paste will not help. The message names the offending FLAG and the offending VALUE verbatim, so a panel can highlight the field the user must fix.",
      "emittedBy": "python/tan/commands/faultdecode_cmd.py",
      "note": "Exit 2 (ValidationFailure), same as the sibling `faultdecode.no-registers`, and emitted through the same `_refuse` helper so both refusals agree with the success path about whether stdout is an envelope. Added by tan-cli#616: `_parse_hexint` used `int(text, 16)`, which accepts a leading `-`, so `--cfsr=-8200` rendered `\"0x-0008200\"`, decoded twelve flags that are not set out of Python's two's-complement sign extension, concluded \"Stack overflow\" and exited 0. A fault register is unsigned by construction. Covers all ten register/address flags (--cfsr/--hfsr/--dfsr/--bfar/--mmfar/--mmfsr/--bfsr/--ufsr/--pc/--lr). NOT used for a non-hex value (`--cfsr zz`), which stays a `typer.BadParameter` so the v0.4.1 oracle's exit-code parity for that input is preserved. DIVERGES from alp-sdk's `scripts/alp_cli/faultdecode.py`, which still decodes a negative CFSR at exit 0; the upstream should follow. Pre-consumer -- promote to `frozen` (filling in consumer for real) the moment a consumer binds to it."
    },
    {
      "code": "faultdecode.stdin-truncated",
      "status": "reserved",
      "severity": "warning",
      "consumer": "none -- no consumer matches this code with `===` yet; registered so a fault-decode panel has a stable spelling to bind to",
      "consumerEffect": "Warns that the implicit stdin dump was NOT read to a clean EOF -- one of the idle/byte-cap/total bounds fired first, and the decode proceeded on whatever arrived. A panel can surface this as a caveat on an otherwise-successful decode: the report is real, but a slower or larger producer may have had more to say. The message names WHICH bound fired and the `--file -` remedy (the explicit, unbounded read-to-EOF path).",
      "emittedBy": "python/tan/commands/faultdecode_cmd.py",
      "note": "Exit 0 on the success path; also reachable alongside `faultdecode.no-registers` at exit 2 when a bound fires WITH bytes read but nothing in them parsed into a complete register set (constraint 4's hole, closed after initial review: a refusal used to announce nothing about the truncation it was itself caused by). Either way this is the sibling of the on-stderr announcement `_stdin_bound_message` writes in EVERY mode, not only `--format json`, per tan-cli#537's own constraint 4 (\"every bound announces, on the default surface\"). Added by tan-cli#537, the implicit-stdin-reader redesign: seven prior attempts on tan-cli#503 each traded one defect (a dropped dump, an unbounded hang, unbounded memory growth, a decode-parity break, a truncation nobody was told about) for another, in the same function. This code is what makes a truncated decode a recoverable inconvenience instead of a confident wrong root cause: `--file -` is the caller's escape hatch when they need the whole thing regardless of how long it takes. DIVERGES from alp-sdk's `scripts/alp_cli/faultdecode.py`, which has no truncation concept at all -- no idle/byte-cap/total bounds on its own stdin read, so nothing there could ever emit this warning or its stderr sibling; the upstream should follow. Pre-consumer -- promote to `frozen` (filling in consumer for real) the moment a consumer binds to it."
    }
  ]
}

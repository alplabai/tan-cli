[project]
# DISTRIBUTION name is `alp-tan`, not `tan`: the name `tan` is already taken on
# PyPI ("The compromising code formatter", v23.7.0), so `pip install tan` would
# fetch an unrelated package. The IMPORT package stays `tan` and the console
# script stays `tan`, so a customer still types `tan build` -- only the install
# line differs:
#
#     pip install alp-tan   ->   tan --version
#
# `tan-cli` was also free; `alp-tan` is preferred because the other Alp Lab
# artefacts are `alp-`prefixed.
name = "alp-tan"
# The PEP 440 RENDERING of `python/tan/version.py`'s TAN_VERSION, which is the
# source of truth -- never edited alone. `0.5.0-rc5.dev0` -> `0.5.0rc5.dev0`
# (the two spellings are not interchangeable and cannot be string-compared);
# `python/scripts/version_check.py` owns that mapping and CI runs it.
version = "0.5.1"
description = "The Alp Lab build CLI: one board.yaml to firmware, across MCU and MPU cores."
readme = "README.md"
# PEP 639 SPDX-expression form, NOT the `{ text = ... }` table -- and it is
# COUPLED to the `setuptools>=77.0.1` floor in `[build-system]` below, which
# exists solely to parse this line (tan-cli#382). Changing either without the
# other is the bug: the table form is what setuptools <77 accepts and what
# setuptools >=77 deprecates, so there is no spelling that satisfies both.
license = "Apache-2.0"
authors = [{ name = "Alp Lab AB" }]
keywords = ["embedded", "firmware", "zephyr", "yocto", "som", "build"]
# 3.12, not 3.11: the floor is what the BUILD needs, not what this package's own
# syntax needs. Zephyr's `cmake/modules/python.cmake` sets
# `PYTHON_MINIMUM_REQUIRED 3.12`, and `tan build` bakes the interpreter it
# resolves into every Zephyr slice as `-DPython3_EXECUTABLE` -- so a `tan` frozen
# against 3.11 hands Zephyr an interpreter its own configure step then rejects,
# and the customer gets an error naming Zephyr rather than tan. Same effective
# floor `tan doctor` enforces (`ZEPHYR_PYTHON_FLOOR`, tan/commands/doctor_cmd.py).
requires-python = ">=3.12"
# `pyyaml` is load-bearing, not optional: `build/system-manifest.yaml` is the ONLY
# input `tan image` and `tan size` have, and it is YAML. Without it declared here
# the PyInstaller build -- which installs exactly this set into a clean venv, see
# scripts/build_binary.sh -- ships a frozen `tan` whose `size`/`image` can never
# read a manifest. Both still degrade to a coded `*.manifest-invalid` envelope
# rather than a traceback if it is somehow absent (`tan.core.system_manifest.
# load_yaml_document`), but that is a safety net, not the plan. It is now doubly
# load-bearing: the relocated planner (`tan.planner`) reads every `metadata/**`
# YAML through it.
#
# `jsonschema` arrived WITH the planner and is the fourth runtime dependency.
# The four-name ceiling (typer, rich, pytest, pyinstaller) was scoped to the
# executor stage; the planner validates every `board.yaml` against
# `metadata/schemas/board.schema.json` under Draft 2020-12 before it plans
# anything (`tan/planner/loader.py`), and that schema is where invariants I-02
# (no top-level `os:`) and I-03 (`core_entry` is closed) are actually ENFORCED.
# Reimplementing the validator to save a dependency would move two enforced
# invariants into hand-written code -- the opposite of consume-what-exists. The
# planner `sys.exit`s with an install hint if it is absent and `tan build` turns
# that into a coded `build.plan-unavailable` envelope, never a traceback.
#
# `click` is the fifth, and it is declared because `tan/cli.py` and
# `tan/commands/new_som_cmd.py` IMPORT IT DIRECTLY -- `from click.testing
# import CliRunner` is how `--help` under `--format json` is captured into one
# envelope instead of Click printing straight to stdout and calling
# `ctx.exit(0)` before `emit()` can run (`_emit_help_envelope`). It rode along
# transitively for years because typer depended on click; starting at **typer
# 0.26.0** it does not (measured: PyPI `requires_dist` for 0.25.1 still lists
# `click>=8.2.1`; 0.26.0's does not, and its wheel gained a vendored
# `typer/_click/` package -- typer's OWN click-alike, not a re-export of the
# public one). So a clean `pip install alp-tan` produced `ModuleNotFoundError:
# No module named 'click'` on first run, and every local test suite passed
# regardless because click was present for other reasons. A package that
# imports a module must declare it; leaning on someone else's dependency graph
# is what broke. `tests/gates/test_declared_dependencies.py` fails if any
# module under `tan/` imports a distribution this list omits. `click` stays
# required even though typer no longer needs it transitively -- the two direct
# imports above are real, not incidental.
#
# `typer` is capped, not just floored, and the cap is the point of this whole
# comment. `tan/cli.py:_emit_help_envelope` drives `typer.main.get_command()`'s
# result -- a `TyperCommand` -- through the PUBLIC `click.testing.CliRunner`.
# Measured: `type(get_command(app)).__mro__` descends from
# `typer._click.core.Command`, typer's PRIVATE vendored fork, not from
# `click.core.Command` -- so this mixes two different click implementations,
# and it only works because the vendored `UsageError.show()` still exits via
# `SystemExit`, which `CliRunner` catches generically (see the docstring at
# that function). That is stable across the vendored-click line -- measured
# clean (`tests/test_cli_skeleton.py` + `tests/gates`) against both 0.26.0 and
# 0.27.0, the only two releases of that line so far -- but it is an
# implementation detail of typer's private fork, not a contract, so nothing
# says a `0.28` won't change what `get_command()` returns or how its group
# raises. Uncapped, a resolver is free to pick that day's newest typer for a
# `pip install ./python` -- the documented path on win32/arm64 and
# linux/arm64, the two hosts with no frozen asset -- and hand it a CLI built
# against a private surface that may no longer exist.
#
# The floor moved off `>=0.12` for the same "measure, don't guess" reason:
# `typer==0.12.0` on PyPI is a metapackage stub (`requires_dist` is just
# `typer-slim[standard]==0.12.0` + `typer-cli==0.12.0`), not a working install
# of the real `typer` module -- a floor including it was already claiming
# support that cannot work. `>=0.26` also makes the floor mean something: every
# version it admits is the SAME vendored-click behaviour this whole comment
# and `tests/gates/test_typer_click_bound.py` are about, not two different
# regimes (pre- and post-vendoring) blurred into one range. Pre-0.26 typer may
# well still work -- it was not the question this port needed answered.
#
# `tests/gates/test_typer_click_bound.py` fails if the installed typer drifts
# outside `<0.28`, or if the private surface this range assumes (`TyperCommand`
# descending from `typer._click.core.Command`, and a `SystemExit`-raising
# `UsageError`) goes away from an in-range version -- so a version bump here is
# a decision, not a drift.
#
# `truststore` and `certifi` are REQUIRED, not optional, and both are new
# (tan-cli#304): a PyInstaller `--onefile` freeze bundles its own `ssl` but no
# CA bundle and does not fall back to the platform trust store, so a frozen
# `tan` had ZERO trust anchors -- every `urllib` HTTPS call in `sdk_cmd.py`
# failed `CERTIFICATE_VERIFY_FAILED` even on a host where `curl`/a browser/
# system `python3` verified the same endpoint fine. `truststore` is preferred
# (verifies through the OS's own trust store, so a real corporate CA installed
# in the machine's keychain still works -- `certifi`'s fixed public-CA list
# alone would trade that failure for a different one); `certifi`'s bundled
# `cacert.pem` is the floor `tan/net.py` falls back to if `truststore` is
# absent or its platform verifier fails. Both are module-scope imports inside
# `tan/net.py`, so both must be REQUIRED per the reasoning right below this
# comment.
dependencies = [
  "typer>=0.26,<0.28",
  "rich>=13",
  "pyyaml>=6",
  "jsonschema>=4.18",
  "click>=8.1",
  "truststore>=0.9",
  "certifi>=2024.2.2",
]

# `pyserial` is OPTIONAL, not required: only `tan monitor` needs it, and a serial
# console is not worth adding to every install of a build tool. `monitor_cmd`
# imports it lazily inside the two functions that use it and refuses with an
# actionable message when it is absent, so `tan --version` and every other
# command still work without it. That laziness is load-bearing, not incidental --
# a module-scope `import serial` would make the whole CLI unimportable on a clean
# install the moment `monitor` is registered, so
# `tests/gates/test_declared_dependencies.py` fails if an extras-only import
# migrates to module scope.
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development :: Build Tools",
  "Topic :: Software Development :: Embedded Systems",
]


# LAST of the `[project]` content on purpose. A table header ends the parent
# table, so every bare `key = ...` after it belongs to the SUB-table, not to
# `[project]`. Putting this above `classifiers` silently reparented it into the
# extras group -- `optional-dependencies` grew a bogus `classifiers` extra,
# `project.classifiers` vanished from the metadata, and `pip install ./python`
# failed outright. Keep new `[project]` KEYS above this line and new
# `[project.*]` TABLES below it.
[project.optional-dependencies]
monitor = ["pyserial>=3.5"]

[project.urls]
Homepage = "https://github.com/alplabai/tan-cli"
Repository = "https://github.com/alplabai/tan-cli"
Issues = "https://github.com/alplabai/tan-cli/issues"

[project.scripts]
tan = "tan.__main__:main"

[build-system]
# 77.0.1, measured, not guessed (tan-cli#382). `>=68` was a bound nobody had
# ever built at: PEP 639 support -- the `license = "Apache-2.0"` string form at
# the top of this file -- landed in the setuptools 77 line, so every version
# `>=68` admitted below that REJECTS this file's own metadata:
#
#     pip install setuptools==76.1.0
#     pip wheel --no-deps --no-build-isolation ./python
#     configuration error: `project.license` must be valid exactly by one
#     definition (2 matches found)  <- expected `file` or `text`
#
# Measured on py3.12.10: 68.1.2 FAIL, 76.1.0 FAIL (the release immediately
# below the floor), 77.0.1 PASS. Build ISOLATION is what hid it for two years
# -- pip then downloads the newest setuptools regardless of this line, so every
# other install path in this repo (`pip install -e ./python` in ci.yml, the
# venvs in release.yml / python-binaries.yml / clean-host.yml) passes no matter
# what number is written here. Only a downstream packager pinned to a valid
# `>=68` environment ever saw it, and they cannot build the distribution at all.
#
# 77.0.1 and not `>=77`: setuptools 77.0.0 and 77.0.2 have NO files on PyPI, so
# `==77` (which PEP 440 zero-pads to 77.0.0) resolves to nothing. 77.0.1 is the
# first release of that line a resolver can actually install, which makes this
# bound an exact version the `wheel-floor` job in .github/workflows/ci.yml can
# pin and build at -- that job derives its pin FROM this string, so a future
# change here is tested at its own floor with nothing else to update.
requires = ["setuptools>=77.0.1"]
build-backend = "setuptools.build_meta"

# Explicit, so `tests/` can never be shipped by an auto-discovery change and a
# new subpackage is never silently omitted from the wheel.
[tool.setuptools.packages.find]
where = ["."]
include = ["tan*"]

# `tan init`'s scaffold trees are DATA, not code, so nothing in the import graph
# pulls them into a wheel on its own -- and without them five of the six
# templates fail (`init.template-unreadable`). The frozen binary needs the
# equivalent `--add-data`, see `scripts/build_binary.sh`.
[tool.setuptools.package-data]
"tan.templates" = ["vendored/**/*"]

[tool.pytest.ini_options]
pythonpath = ["."]

# tan-cli#408. ADVISORY ONLY -- read this before believing it enforces
# anything. No workflow under `.github/workflows/` runs ruff, so this section
# changes exactly one thing: an editor with ruff wired in now surfaces the
# three complexity rules while you type, instead of nothing at all.
#
# What actually ENFORCES the size guideline is
# `tests/gates/test_module_size_budget.py`, a ratchet that runs inside
# `python -- pytest across python/` -- already a required context on `main`
# and `dev`, so it gates merges today with no branch-protection change. A new
# required CI job would have to be added to that list to matter, and adding a
# required context blocks every open PR until it has run on each of them.
#
# `ruff check python/` is red on this tree, deliberately and honestly: 23
# modules are over 800 lines and 199 functions over 50. The gate records that
# as a baseline and fails on GROWTH; ruff here reports the standing debt.
[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# PLR0911 too-many-return-statements, PLR0912 too-many-branches,
# PLR0915 too-many-statements -- the three `bootstrap_cmd._run`'s lone
# `# noqa` names, and the only `noqa: PLR` anywhere under `tan/`.
extend-select = ["PLR0911", "PLR0912", "PLR0915"]

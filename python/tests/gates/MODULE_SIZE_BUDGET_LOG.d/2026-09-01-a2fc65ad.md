- 2026-09-01 -- tan-cli#1053 + tan-cli#1051 (incl. the review round): route sdk remove's FIVE load-bearing path comparisons through the shared, symlink-asymmetric removal_would_take_out and thread both the pre-removal registry match set and the dangling-project-pin warning through _run_remove's seven emit/fail branches. Honest split of the +154 net lines in sdk_cmd.py: 75 net are pure '#' comment (79 added, 4 removed) -- the two section banners carrying the platform reasoning, the symlink asymmetry measured in review, and the narrow-vs-wide tier divergence -- and 69 net are code/docstring, of which the structural core is one 'after = _resolves_to_after(...)' binding plus one 'issues=after.issues' argument per branch, the _registered_entries_for/_load_bearing_reasons/_prune_registry_entries_for signature changes, and the islink veto. The worst_function 229 -> 255 half is entirely structural: _run_remove gained only bindings and arguments, no prose. Docstring growth that pushed _resolves_to_after and prune_entries_by_sdk_path over FUNCTION_CAP was TRIMMED rather than bought -- derived function_count_budget stays 300
    - tan/commands/sdk_cmd.py: 1530 -> 1684

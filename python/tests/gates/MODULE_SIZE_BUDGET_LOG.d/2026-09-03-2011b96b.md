- 2026-09-03 -- issue #1133 (PR pending): route tan/planner/template.py's two metadata/** reads through DocumentGuards.require_readable_text plus a new local YAML-parse guard, so a non-UTF-8 byte, an unparseable document and an unreadable file each produce a curated TemplateError instead of the raw UnicodeDecodeError/yaml.ParserError/PermissionError measured escaping emit_scaffold on 3.12.3, 3.13.15 and 3.14.7; the is_file() pre-flight is deleted rather than widened because it is itself the tan-cli#1127 trap (raises on 3.12/3.13, returns False on 3.14); the growth is the two new module-level helpers (_parse_yaml_mapping, _read_yaml_mapping) with the measurements that justify each, plus the corrected load_catalog docstring paragraph whose is_file() claim this change falsifies
    - tan/planner/template.py: 2125 -> 2219

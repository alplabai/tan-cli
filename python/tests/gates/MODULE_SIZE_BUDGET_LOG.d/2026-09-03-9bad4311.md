- 2026-09-03 -- issue #1143 review round 2 (PR #1148): the rejected-token warning moved BELOW resolve+stage so a registered issue code can no longer assert a download went out unauthenticated when it went out authenticated on the next variable down (shadowed_sdk_token_vars + unusable_token_message render from the outcome); the credential sweep gained liveness discrimination (SDK_CREDENTIAL_LIVE_WINDOW_S + _sdk_credential_is_possibly_live) so a concurrent tan bootstrap cannot silently downgrade another run by deleting its live netrc. toolchain_provision.py crosses MODULE_CAP for the first time at 849 -- the pure half of both fixes plus two docstring corrections (the /tmp escalation claim was overstated; a runtime west-assumption check is viable and is a filed follow-up, only a CI-time one is not)
    - tan/commands/bootstrap_cmd.py: 4364 -> 4423
    - tan/core/toolchain_provision.py: new entry at 849

- 2026-09-03 -- tan-cli#1118 planner re-sync alp-sdk 0914da38 -> ff27f179: hand-ported alp-sdk#1855 (alp-sdk 5c33ef04) into tan/planner/template.py -- new _BARE_REPO_PATH_RE + _scaffold_bare_repo_paths, the optional trailing-subpath capture in _scaffold_readme, and the call wired into render_to_envelope's board.yaml arm plus a new .c/.h arm. +70/-8, exactly the upstream patch's own +70/-8, because the delta transferred 1:1 into tan's structure. render_to_envelope's span 241 -> 257 against FUNCTION_CAP 50 and the module 2063 -> 2125 against MODULE_CAP 800; both are already tan-cli#1142's subject and both were far over before this change. Splitting either here would make the diff unreadable against the single upstream patch a hand-port audit exists to be readable against, so the growth is taken deliberately and #1142 lands on top of it.
    - tan/planner/template.py: 2063 -> 2125

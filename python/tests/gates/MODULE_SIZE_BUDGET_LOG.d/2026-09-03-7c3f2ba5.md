- 2026-09-03 -- tan-cli#1162: route the last four absent-try metadata reads in tan/planner/** (libraries.load_manifest, zephyr_board._load_soc_spec, kconfig_symbols._load_board_symbols, topology._core_os_choices) through tan/core/document_guards.py instead of an is_file() pre-flight plus a bare read+parse; the two raises are traced to what actually moved each counter rather than to the change as a whole. zephyr_board.py's +29 is _load_soc_spec's guarded read plus its RELOCATED-divergence docstring, which records that the site is NOT on the tan build path (correcting #1162's own body), which handler really catches it, and what it produced on 3.12.3/3.13.15/3.14.7 before the fix. function_count_budget +1 is topology._core_os_choices alone, crossing FUNCTION_CAP=50 at 63 lines: the require_key walk of the $defs chain that replaces five bare subscripts, plus the docstring for the pre-flight that was a document SELECTOR rather than a guard. The three new register methods (read_optional_text, require_json_mapping_doc, read_json_mapping) contributed ZERO to either counter and are named here only to say so: they live in tan/core/document_guards.py, which has no record under module_size_budget.d/ on either side of the change and appears in neither the measured modules nor the measured functions map at base or head. Measured on the branch END STATE, which is what a squash-merge lands and the only tree this entry can honestly describe: document_guards.py is 711 lines against MODULE_CAP=800, worst function span 49 (require_readable_text) against FUNCTION_CAP=50, second 46 (require_yaml_mapping_doc); at the commit that raised the two counters above it was 680 and 46. Both stay clear of both caps, so function_count_budget is 311 either way. require_readable_text at 49 is now the worst span in that file and one line under the cap: the next thing that wants to grow it should take a ledger entry, not be trimmed to fit.
    - tan/planner/zephyr_board.py: 1494 -> 1523
    - function_count_budget: 310 -> 311

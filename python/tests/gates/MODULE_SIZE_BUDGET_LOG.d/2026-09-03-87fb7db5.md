- 2026-09-03 -- tan-cli#1170 review of #1154: the falsified netrc mechanism survived in the only user-facing string that carried it (_AUTHENTICATED_QUOTA_NOTE told the reader a west Authorization header would bypass tan's netrc; measured on requests 2.34.2 the netrc match overwrites it, so the shapes named now are auth= and trust_env=False). toolchain_provision.py grows those two facts into west_sdk_netrc_assumptions_hold plus WEST_SDK_TOKEN_FLAG_ATTR, so the tripwire guards the vectors it was built for rather than one that is not tan-inert at all, records both regexes' measured trips and silences on _WEST_SDK_ENV_READ and _WEST_SDK_REQUESTS_AUTH_ARG, and adds _UNVERIFIED_QUOTA_NOTE -- rate_limit_note's fourth state, so a staged-but-unverified credential is not told minutes later that tan already authenticated the download. bootstrap_cmd.py carries that verdict on _SdkCredential, replaces a false --no-west justification with the cases that can actually reach an unreadable sdk.py, and grows _zephyr_base_path 13 -> 24 lines for the text-not-Path guard that keeps a declared trailing slash unnormalised on the envelope and stops a blank $ZEPHYR_BASE becoming a CWD-relative sdk.py read. FUNCTION_CAP offset recorded here because the whole-tree scalar hides it: bootstrap_cmd.py's long_functions stayed at 19 across the whole of #1154 only because two movements cancelled -- _sdk_credential crossed the 50-line cap (50 at dd6ac839, 63 at 72461d7e, 69 here) while _data fell under it (51 -> 47) when _zephyr_base_path was extracted. This round crosses nothing further: _stage_sdk_credential measures 50 in every committed tree and west_sdk_netrc_assumptions_hold lands at 50, grown from 27, with the material that would have taken it past the cap relocated to _WEST_SDK_ENV_READ's comment block rather than deleted.
    - tan/commands/bootstrap_cmd.py: 4507 -> 4548
    - tan/core/toolchain_provision.py: 955 -> 1094

- 2026-09-16 -- tan-cli#1262 round 2: correct the exit-2 reachability claim (the SCRIPT never returns 2, but the spawned PROCESS still exits 2 when the interpreter cannot open the script -- measured), widen diff_cmd's docstring enumeration with the two new sibling codes, document that validatorExitStatus is the raw returncode and negative on POSIX signal death, and record diff's knowingly-deferred lossiness at _data()
    - tan/commands/diff_cmd.py: 890 -> 911
    - tan/commands/validate_cmd.py: 1775 -> 1793
    - tan/commands/validate_cmd.py:_emit: 205 -> 213

- 2026-09-01 -- tan-cli#1053 + tan-cli#1051: route sdk remove's four load-bearing path comparisons through the shared platform-aware names_the_same_directory (a plain == let a second spelling of one directory through and orphaned the project), and thread the dangling-project-pin warning onto every one of _run_remove's seven emit/fail branches -- the growth is one 'after = _resolves_to_after(...)' binding plus one 'issues=after.issues' argument per branch, not prose
    - tan/commands/sdk_cmd.py: 1530 -> 1620

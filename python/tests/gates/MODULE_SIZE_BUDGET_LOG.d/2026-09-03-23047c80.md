- 2026-09-03 -- issue #1133 review round 2 (PR #1160): curate the three further template.py escapes the review drove -- _rendered_bytes' bare per-file read_bytes() (raw PermissionError/FileNotFoundError/IsADirectoryError out of emit_scaffold on 3.12.3, 3.13.15 and 3.14.7, and the busiest read in the module at 4-7 files per scaffold), its substitution-branch .decode (raw UnicodeDecodeError, latent: no shipped catalog can declare a substitute:), and _safe_join's Path.resolve(), which raises a bare RuntimeError on a symlink loop on 3.12.3 and returns the path unchanged on 3.13.15/3.14.7. WHY THE FUNCTION_CAP CROSSING, measured rather than asserted (this corrects the first draft of this entry, which called it code growth and was wrong): _rendered_bytes goes 41 -> 54 lines, of which +6 is code (the guarded read is two lines where one was; the decode needs a six-line try/except) and +7 is the review paragraph at :536-542. Without that paragraph the function is 47, under FUNCTION_CAP = 50, so the paragraph is the cause of the crossing and the guard is not. It is kept anyway, and not shaved to sit at 49: refusing to shrink the guard and then shrinking the prose to land under the cap would be the same metric-chasing in the other direction. Every other over-cap function in this module is over for teaching prose -- that is the module's established shape -- and this paragraph is already the trimmed form, with the per-cell measurement moved to tests/planner/test_emit_scaffold_unreadable_metadata.py; what is left is the irreducible pointer: which site this is, how hot it is, that the decode beside it was a latent fifth, and where the evidence lives. The two YAML helpers this PR added in round one moved OUT to tan/core/document_guards.py in the same round (format symmetry with read_catalog_document; the import-closure objection was measurably wrong), so template.py is 2219 -> 2206 net, still +81 over dev
    - function_count_budget: 309 -> 310

- 2026-09-04 -- tan-cli#1189: restore_ld_library_path gains a second trigger, because an absent LD_LIBRARY_PATH_ORIG is not proof that no override happened -- measured leaking the bundled liblzma.so.5 into tar/xz on a bare ubuntu:24.04. bootstrap_cmd.py grows 5 lines: 4 for the multi-line import of the new ld_library_path_needs_restore predicate (a single-line import is 101 chars against the 100 limit) and 1 for the comment pointing Runner._env's fast path at it. The two functions that crossed FUNCTION_CAP were extracted rather than budgeted -- restore_ld_library_path split into _restore_from_orig + _strip_bundle_dir (53 to 18 lines) and Runner._env trimmed back to 49.
    - tan/commands/bootstrap_cmd.py: 4548 -> 4553

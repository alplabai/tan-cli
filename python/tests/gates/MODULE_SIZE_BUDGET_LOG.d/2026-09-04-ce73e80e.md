- 2026-09-04 -- tan-cli#1194 review: the OpenOCD no-serverpath note's wording is split three ways so each spelling is true in the state it is emitted in. The first version asserted 'this project's runners.yaml has no config.openocd key' in every state, including the pre-build state -- where _resolve_from_build returns at its missing-manifest branch and never opens a runners.yaml -- which is the command's own primary documented use AND the state the debug-config-preview-baremetal-mcu contract golden records, surfaced VERBATIM by alp-sdk-vscode. debug_config_cmd.py gains two more note constants plus a shared advice constant, two more arms in _openocd_hosttools_note, and the block comment recording the registered_runners signal and why baremetal-mcu (a plain-CMake backend) gets no Zephyr-SDK paragraph. No function crosses FUNCTION_CAP: the reasoning stays a module-scope comment, _openocd_hosttools_note spans 19 lines and _preview_notes_for 32, and long_functions/worst_function are unchanged.
    - tan/commands/debug_config_cmd.py: 2295 -> 2375
